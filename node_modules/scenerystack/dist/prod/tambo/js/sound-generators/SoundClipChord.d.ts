/**
 * SoundClipChord plays a chord built from the given sound.
 *
 * @author Michael Kauzmann (PhET Interactive Simulations)
 */
import '../../../globals.js';
import { TReadOnlyProperty } from '../../../axon/js/TReadOnlyProperty.js';
import { SoundClipOptions } from '../../../tambo/js/sound-generators/SoundClip.js';
import SoundGenerator, { SoundGeneratorOptions } from '../../../tambo/js/sound-generators/SoundGenerator.js';
import TSoundPlayer from '../TSoundPlayer.js';
import WrappedAudioBuffer from '../WrappedAudioBuffer.js';
type SelfOptions = {
    soundClipOptions?: SoundClipOptions | null;
    arpeggiate?: boolean;
    arpeggiateTime?: number;
    chordPlaybackRates?: number[];
};
export type SoundClipChordOptions = SelfOptions & SoundGeneratorOptions;
declare class SoundClipChord extends SoundGenerator implements TSoundPlayer {
    private readonly arpeggiate;
    private readonly arpeggiateTime;
    private readonly playbackSoundClips;
    readonly isPlayingProperty: TReadOnlyProperty<boolean>;
    constructor(sound: WrappedAudioBuffer, providedOptions?: SoundClipChordOptions);
    /**
     * Play the chord.
     */
    play(): void;
    /**
     * Stop the chord if it's playing.  This is mostly here to complete the TSoundPlayer interface.
     */
    stop(): void;
    /**
     * Release any memory references in order to avoid memory leaks.
     */
    dispose(): void;
}
export default SoundClipChord;
//# sourceMappingURL=SoundClipChord.d.ts.map