/**
 * Creates a quad for a given texture with a specific width and height, in the 3d space
 * with x: [0,width] and y: [0,height].
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../globals.js';
import * as THREE from 'three';
import { TReadOnlyEmitter } from '../../axon/js/TEmitter.js';
export default class TextureQuad extends THREE.Mesh {
    private readonly quadGeometry;
    private readonly basicMaterial;
    private textureQuadWidth;
    private textureQuadHeight;
    private disposable;
    disposeEmitter: TReadOnlyEmitter;
    constructor(texture: THREE.Texture, width: number, height: number, materialOptions?: THREE.MaterialParameters);
    updateTexture(texture: THREE.Texture, width?: number, height?: number): void;
    /**
     * Releases references
     */
    dispose(): void;
}
//# sourceMappingURL=TextureQuad.d.ts.map