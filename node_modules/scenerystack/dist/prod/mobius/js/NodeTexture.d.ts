/**
 * Allows turning a Node into an updatable three.js texture.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../globals.js';
import * as THREE from 'three';
import Node from '../../scenery/js/nodes/Node.js';
type NodeTextureOptions = {
    width?: number;
    height?: number;
    calculateDimensionFromNode?: boolean;
};
export default class NodeTexture extends THREE.Texture {
    private _display;
    private _scene;
    _width: number;
    _height: number;
    private disposable;
    constructor(node: Node, providedOptions?: NodeTextureOptions);
    /**
     * Updates the node's appearance in the texture.
     */
    update(): void;
    /**
     * Releases references.
     */
    dispose(): void;
}
export {};
//# sourceMappingURL=NodeTexture.d.ts.map