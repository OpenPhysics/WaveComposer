/**
 * Data pertaining to a hotkey, including keystrokes and associated metadata for documentation and the keyboard help
 * dialog.
 *
 * @author Jesse Greenberg (PhET Interactive Simulations)
 */
import '../../../globals.js';
import '../../../chipper/js/browser/initialize-globals.js';
import { TReadOnlyProperty } from '../../../axon/js/TReadOnlyProperty.js';
import type { OneKeyStroke } from './KeyDescriptor.js';
import KeyDescriptor from './KeyDescriptor.js';
type SerializedHotkeyData = {
    keyStrings: string[];
    repoName: string;
    binderName: string;
    global: boolean;
};
export type HotkeyDataOptions = {
    keys: Array<TReadOnlyProperty<OneKeyStroke> | OneKeyStroke>;
    keyboardHelpDialogLabelStringProperty?: TReadOnlyProperty<string> | null;
    keyboardHelpDialogPDOMLabelStringProperty?: TReadOnlyProperty<string> | string | null;
    repoName: string;
    global?: boolean;
    binderName?: string;
};
export default class HotkeyData {
    readonly keyStringProperties: TReadOnlyProperty<OneKeyStroke>[];
    readonly keyboardHelpDialogLabelStringProperty: TReadOnlyProperty<string> | null;
    readonly keyboardHelpDialogPDOMLabelStringProperty: TReadOnlyProperty<string> | string | null;
    readonly keyDescriptorsProperty: TReadOnlyProperty<KeyDescriptor[]>;
    private readonly repoName;
    private readonly global;
    private readonly binderName;
    constructor(providedOptions: HotkeyDataOptions);
    /**
     * Returns true if any of the keys of this HotkeyData have the given keyStroke.
     */
    hasKeyStroke(keyStroke: OneKeyStroke): boolean;
    /**
     * Serialization for usage with binder (generated documentation).
     */
    serialize(): SerializedHotkeyData;
    /**
     * Dispose of owned Properties to prevent memory leaks.
     */
    dispose(): void;
    /**
     * Combine the keys of an array of HotkeyData into a single array. Useful if you want to combine
     * multiple HotkeyData for a single KeyboardListener.
     */
    static combineKeyStringProperties(hotkeyDataArray: HotkeyData[]): TReadOnlyProperty<OneKeyStroke>[];
    /**
     * Returns true if any of the HotkeyData in the array have the given keyStroke.
     */
    static anyHaveKeyStroke(hotkeyDataArray: HotkeyData[], keyStroke: OneKeyStroke): boolean;
}
export {};
//# sourceMappingURL=HotkeyData.d.ts.map