/**
 * A link node in RichText - NOTE: This is NOT embedded for layout. Instead, link content will be added as children to this node,
 * and this will exist solely for the link functionality.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
import Pool, { TPoolable } from '../../../../phet-core/js/Pool.js';
import Node from '../../nodes/Node.js';
import type { RichTextHref } from '../../nodes/RichText.js';
declare const RichTextLink_base: typeof Node & import("../../../../phet-core.js").Constructor<{
    readonly isCleanable: boolean;
    clean(): void;
}> & import("../../../../phet-core.js").Constructor<import("../../accessibility/voicing/Voicing.js").TVoicing<Node & {
    readonly isCleanable: boolean;
    clean(): void;
}>>;
export default class RichTextLink extends RichTextLink_base implements TPoolable {
    private fireListener;
    private accessibleInputListener;
    private allowLinksListener;
    constructor(innerContent: string, href: RichTextHref);
    /**
     * Set up this state. First construction does not need to use super.initialize() because the constructor has done
     * that for us. But repeated initialization with Poolable will need to initialize super again.
     */
    initialize(innerContent: string, href: RichTextHref, initializeSuper?: boolean): this;
    /**
     * Cleans references that could cause memory leaks (as those things may contain other references).
     */
    clean(): void;
    freeToPool(): void;
    static readonly pool: Pool<typeof RichTextLink>;
}
export {};
//# sourceMappingURL=RichTextLink.d.ts.map