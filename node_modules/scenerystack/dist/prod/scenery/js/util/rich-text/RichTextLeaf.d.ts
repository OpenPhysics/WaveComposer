/**
 * A leaf (text) element in the RichText, which will display a snippet of Text.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
import Pool, { TPoolable } from '../../../../phet-core/js/Pool.js';
import Font from '../../util/Font.js';
import type { TextBoundsMethod } from '../../nodes/Text.js';
import Text from '../../nodes/Text.js';
import TPaint from '../../util/TPaint.js';
declare const RichTextLeaf_base: typeof Text & import("../../../../phet-core.js").Constructor<{
    readonly isCleanable: boolean;
    clean(): void;
}>;
export default class RichTextLeaf extends RichTextLeaf_base implements TPoolable {
    leftSpacing: number;
    rightSpacing: number;
    constructor(content: string, isLTR: boolean, font: Font | string, boundsMethod: TextBoundsMethod, fill: TPaint, stroke: TPaint, lineWidth: number);
    initialize(content: string, isLTR: boolean, font: Font | string, boundsMethod: TextBoundsMethod, fill: TPaint, stroke: TPaint, lineWidth: number): this;
    /**
     * Cleans references that could cause memory leaks (as those things may contain other references).
     */
    clean(): void;
    /**
     * Whether this leaf will fit in the specified amount of space (including, if required, the amount of spacing on
     * the side).
     */
    fitsIn(widthAvailable: number, hasAddedLeafToLine: boolean, isContainerLTR: boolean): boolean;
    freeToPool(): void;
    static readonly pool: Pool<typeof RichTextLeaf>;
}
export {};
//# sourceMappingURL=RichTextLeaf.d.ts.map