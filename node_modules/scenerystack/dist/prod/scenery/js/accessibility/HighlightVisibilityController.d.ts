/**
 * A listener that manages the visibility of different highlights when switching between mouse/touch and alternative
 * input for a Display.
 *
 * @author Jesse Greenberg (PhET Interactive Simulations)
 */
import '../../../globals.js';
import type Display from '../display/Display.js';
import TProperty from '../../../axon/js/TProperty.js';
export type HighlightVisibilityControllerOptions = {
    interactiveHighlightsEnabledProperty?: TProperty<boolean>;
};
declare class HighlightVisibilityController {
    private readonly display;
    private readonly disposeHighlightVisibilityController;
    private initialPointerPoint;
    private relativePointerDistance;
    constructor(display: Display, providedOptions: HighlightVisibilityControllerOptions);
    /**
     * Switches between focus highlights and Interactive Highlights if there is enough mouse movement.
     */
    private handleMove;
    dispose(): void;
}
export default HighlightVisibilityController;
//# sourceMappingURL=HighlightVisibilityController.d.ts.map