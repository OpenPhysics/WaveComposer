export default CircleCanvasDrawable;
declare const CircleCanvasDrawable_base: {
    new (): {
        [x: string]: any;
        initialize(renderer: number, instance: Instance, ...args: any[]): void;
        fillCallback: any;
        strokeCallback: any;
        fillObserver: any;
        strokeObserver: any;
        dispose(): void;
        markDirtyFill(): void;
        markDirtyStroke(): void;
        markDirtyLineWidth(): void;
        markDirtyLineOptions(): void;
        markDirtyCachedPaints(): void;
    };
    [x: string]: any;
};
declare class CircleCanvasDrawable extends CircleCanvasDrawable_base {
    /**
     * Paints this drawable to a Canvas (the wrapper contains both a Canvas reference and its drawing context).
     * @public
     *
     * Assumes that the Canvas's context is already in the proper local coordinate frame for the node, and that any
     * other required effects (opacity, clipping, etc.) have already been prepared.
     *
     * This is part of the CanvasSelfDrawable API required to be implemented for subtypes.
     *
     * @param {CanvasContextWrapper} wrapper - Contains the Canvas and its drawing context
     * @param {Node} node - Our node that is being drawn
     * @param {Matrix3} matrix - The transformation matrix applied for this node's coordinate system.
     */
    public paintCanvas(wrapper: CanvasContextWrapper, node: Node, matrix: Matrix3): void;
    /**
     * Called when the radius of the circle changes.
     * @public
     */
    public markDirtyRadius(): void;
}
import Node from '../../nodes/Node.js';
//# sourceMappingURL=CircleCanvasDrawable.d.ts.map