export default BackboneDrawable;
declare class BackboneDrawable extends Drawable {
    /**
     * Creates a base DOM element for a backbone.
     * @public
     *
     * @returns {HTMLDivElement}
     */
    public static createRootDivBackbone(): HTMLDivElement;
    /**
     * Creates a DOM element for a backbone.
     * @public
     *
     * @returns {HTMLDivElement}
     */
    public static createDivBackbone(): HTMLDivElement;
    /**
     * Given an external element, we apply the necessary style to make it compatible as a backbone DOM element.
     * @public
     *
     * @param {HTMLElement} element
     * @returns {HTMLElement} - For chaining
     */
    public static repurposeBackboneContainer(element: HTMLElement): HTMLElement;
    /**
     * @mixes Poolable
     *
     * @param {Display} display
     * @param {Instance} backboneInstance
     * @param {Instance} transformRootInstance - All transforms of this instance and its ancestors will already have been
     *                                           applied. This block will only be responsible for applying transforms of
     *                                           this instance's descendants.
     * @param {number} renderer
     * @param {boolean} isDisplayRoot
     */
    constructor(display: Display, backboneInstance: Instance, transformRootInstance: Instance, renderer: number, isDisplayRoot: boolean);
    /**
     * @public
     *
     * @param {Display} display
     * @param {Instance} backboneInstance
     * @param {Instance} transformRootInstance
     * @param {number} renderer
     * @param {boolean} isDisplayRoot
     */
    public initialize(display: Display, backboneInstance: Instance, transformRootInstance: Instance, renderer: number, isDisplayRoot: boolean): void;
    display: any;
    backboneInstance: any;
    transformRootInstance: any;
    filterRootAncestorInstance: any;
    transformRootAncestorInstance: any;
    willApplyTransform: boolean | undefined;
    willApplyFilters: boolean | undefined;
    transformListener: any;
    backboneVisibilityListener: any;
    visibilityDirty: boolean | undefined;
    domElement: any;
    isDisplayRoot: boolean | undefined;
    dirtyDrawables: any;
    watchedFilterNodes: any;
    filterDirty: boolean | undefined;
    clipDirty: boolean | undefined;
    filterDirtyListener: any;
    clipDirtyListener: any;
    lastZIndex: number | undefined;
    blocks: any;
    previousFirstDrawable: any;
    previousLastDrawable: any;
    removedDrawables: boolean | undefined;
    stitcher: any;
    /**
     * Dispose all of the blocks while clearing our references to them
     * @public
     */
    public markBlocksForDisposal(): void;
    /**
     * @private
     */
    private updateBackboneVisibility;
    /**
     * Marks a drawable as dirty.
     * @public
     *
     * @param {Drawable} drawable
     */
    public markDirtyDrawable(drawable: Drawable): void;
    /**
     * Marks our transform as dirty.
     * @public
     */
    public markTransformDirty(): void;
    /**
     * Marks our opacity as dirty.
     * @private
     */
    private onFilterDirty;
    /**
     * Marks our clip as dirty.
     * @private
     */
    private onClipDirty;
    /**
     * Returns the combined visibility of nodes "above us" that will need to be taken into account for displaying this
     * backbone.
     * @public
     *
     * @returns {boolean}
     */
    public getFilterVisibility(): boolean;
    /**
     * Returns the combined clipArea (string???) for nodes "above us".
     * @public
     *
     * @returns {string}
     */
    public getFilterClip(): string;
    /**
     * Ensures that z-indices are strictly increasing, while trying to minimize the number of times we must change it
     * @public
     */
    public reindexBlocks(): void;
    /**
     * Stitches multiple change intervals.
     * @public
     *
     * @param {Drawable} firstDrawable
     * @param {Drawable} lastDrawable
     * @param {ChangeInterval} firstChangeInterval
     * @param {ChangeInterval} lastChangeInterval
     */
    public stitch(firstDrawable: Drawable, lastDrawable: Drawable, firstChangeInterval: ChangeInterval, lastChangeInterval: ChangeInterval): void;
}
import Drawable from '../display/Drawable.js';
//# sourceMappingURL=BackboneDrawable.d.ts.map