export default ChangeInterval;
declare class ChangeInterval {
    /**
     * Creates a ChangeInterval that will be disposed after syncTree is complete (see Display phases).
     * @public
     *
     * @param {Drawable} drawableBefore
     * @param {Drawable} drawableAfter
     * @param {Display} display
     *
     * @returns {ChangeInterval}
     */
    public static newForDisplay(drawableBefore: Drawable, drawableAfter: Drawable, display: Display): ChangeInterval;
    /**
     * @mixes Poolable
     *
     * @param {Drawable} drawableBefore
     * @param {Drawable} drawableAfter
     */
    constructor(drawableBefore: Drawable, drawableAfter: Drawable);
    /**
     * @public
     *
     * @param {Drawable} drawableBefore
     * @param {Drawable} drawableAfter
     */
    public initialize(drawableBefore: Drawable, drawableAfter: Drawable): void;
    nextChangeInterval: any;
    drawableBefore: any;
    drawableAfter: any;
    collapsedEmpty: boolean | undefined;
    /**
     * Releases references
     * @public
     */
    public dispose(): void;
    /**
     * Make our interval as tight as possible (we may have over-estimated it before)
     * @public
     *
     * @returns {boolean} - Whether it was changed
     */
    public constrict(): boolean;
    /**
     * @public
     *
     * @returns {boolean}
     */
    public isEmpty(): boolean;
    /**
     * The quantity of "old" internal drawables. Requires the old first/last drawables for the backbone, since
     * we need that information for null-before/after boundaries.
     * @public
     *
     * @param {Drawable} oldStitchFirstDrawable
     * @param {Drawable} oldStitchLastDrawable
     * @returns {number}
     */
    public getOldInternalDrawableCount(oldStitchFirstDrawable: Drawable, oldStitchLastDrawable: Drawable): number;
    /**
     * The quantity of "new" internal drawables. Requires the old first/last drawables for the backbone, since
     * we need that information for null-before/after boundaries.
     * @public
     *
     * @param {Drawable} newStitchFirstDrawable
     * @param {Drawable} newStitchLastDrawable
     *
     * @returns {number}
     */
    public getNewInternalDrawableCount(newStitchFirstDrawable: Drawable, newStitchLastDrawable: Drawable): number;
}
import Drawable from '../display/Drawable.js';
//# sourceMappingURL=ChangeInterval.d.ts.map