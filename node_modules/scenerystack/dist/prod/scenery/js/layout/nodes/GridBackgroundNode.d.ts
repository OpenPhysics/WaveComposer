/**
 * Displays a background for a given GridConstraint.
 *
 * NOTE: If there are "holes" in the GridBox/GridConstraint (where there is no cell content for an x/y position), then
 * there will be no background for where those cells (if added) would have been.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
import GridCell from '../../layout/constraints/GridCell.js';
import GridConstraint from '../../layout/constraints/GridConstraint.js';
import Node from '../../nodes/Node.js';
import type { NodeOptions } from '../../nodes/Node.js';
import TPaint from '../../util/TPaint.js';
type CreateCellBackground = (gridCell: GridCell) => Node | null;
type SelfOptions = {
    createCellBackground?: CreateCellBackground;
    fill?: TPaint;
    stroke?: TPaint;
};
export type GridBackgroundNodeOptions = SelfOptions & NodeOptions;
export default class GridBackgroundNode extends Node {
    private readonly constraint;
    private readonly createCellBackground;
    private readonly layoutListener;
    constructor(constraint: GridConstraint, providedOptions?: GridBackgroundNodeOptions);
    private update;
    /**
     * Releases references
     */
    dispose(): void;
}
export {};
//# sourceMappingURL=GridBackgroundNode.d.ts.map