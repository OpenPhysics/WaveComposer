/**
 * Like ManualConstraint, but permits layout when not all the nodes are connected (null will be passed through).
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
import IntentionalAny from '../../../../phet-core/js/types/IntentionalAny.js';
import LayoutConstraint from '../../layout/constraints/LayoutConstraint.js';
import LayoutProxy from '../../layout/LayoutProxy.js';
import Node from '../../nodes/Node.js';
type LayoutProxyMap<T> = {
    [Property in keyof T]: LayoutProxy | null;
};
type LayoutCallback<T extends IntentionalAny[]> = (...args: LayoutProxyMap<T>) => void;
export default class RelaxedManualConstraint<T extends Node[]> extends LayoutConstraint {
    private readonly nodes;
    private readonly cells;
    private readonly layoutCallback;
    constructor(ancestorNode: Node, nodes: T, layoutCallback: LayoutCallback<T>);
    /**
     * (scenery-internal)
     */
    layout(): void;
    /**
     * Releases references
     */
    dispose(): void;
    static create<T extends Node[]>(ancestorNode: Node, nodes: T, layoutCallback: LayoutCallback<T>): RelaxedManualConstraint<T>;
}
export {};
//# sourceMappingURL=RelaxedManualConstraint.d.ts.map