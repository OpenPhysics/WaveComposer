/**
 * Function for doing a linear mapping between two domains ('a' and 'b').
 * <p>
 * Example usage:
 * <code>
 * var f = new LinearFunction( 0, 100, 0, 200 );
 * f.evaluate( 50 ); // 100
 * f.inverse( 100 ); // 50
 * </code>
 *
 * @author Chris Malley (PixelZoom, Inc.)
 */
import '../../globals.js';
export default class LinearFunction {
    private a1;
    private a2;
    private b1;
    private b2;
    private clamp;
    /**
     * @param a1
     * @param a2
     * @param b1
     * @param b2
     * @param clamp - clamp the result to the provided ranges, false by default
     */
    constructor(a1: number, a2: number, b1: number, b2: number, clamp?: boolean);
    /**
     * Maps from a to b.
     */
    evaluate(a3: number): number;
    /**
     * Maps from b to a
     */
    inverse(b3: number): number;
}
//# sourceMappingURL=LinearFunction.d.ts.map