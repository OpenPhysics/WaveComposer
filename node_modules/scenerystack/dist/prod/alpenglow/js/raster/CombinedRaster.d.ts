/**
 * An OutputRaster that tries to efficiently write straight to ImageData when possible, and otherwise accumulates.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import Vector4 from '../../../dot/js/Vector4.js';
import type { OutputRaster } from './OutputRaster.js';
import type { RasterColorConverter } from './RasterColorConverter.js';
export type CombinedRasterOptions = {
    colorSpace?: 'srgb' | 'display-p3';
    showOutOfGamut?: boolean;
};
export declare class CombinedRaster implements OutputRaster {
    readonly width: number;
    readonly height: number;
    readonly accumulationArray: Float64Array;
    readonly imageData: ImageData;
    private combined;
    readonly colorConverter: RasterColorConverter;
    constructor(width: number, height: number, providedOptions?: CombinedRasterOptions);
    addClientPartialPixel(color: Vector4, x: number, y: number): void;
    addClientFullPixel(color: Vector4, x: number, y: number): void;
    addOutputFullPixel(color: Vector4, x: number, y: number): void;
    addClientFullRegion(color: Vector4, x: number, y: number, width: number, height: number): void;
    addOutputFullRegion(color: Vector4, x: number, y: number, width: number, height: number): void;
    toImageData(): ImageData;
    toCanvas(): HTMLCanvasElement;
    writeToCanvas(canvas: HTMLCanvasElement, context: CanvasRenderingContext2D): void;
}
//# sourceMappingURL=CombinedRaster.d.ts.map