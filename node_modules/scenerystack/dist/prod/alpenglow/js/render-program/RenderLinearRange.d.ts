/**
 * Represents a section of a gradient's "color stop" space.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import type { RenderProgram } from './RenderProgram.js';
import { RenderExtend } from './RenderExtend.js';
import type { RenderGradientStop } from './RenderGradientStop.js';
export declare class RenderLinearRange {
    readonly start: number;
    readonly end: number;
    readonly startProgram: RenderProgram;
    readonly endProgram: RenderProgram;
    constructor(start: number, end: number, startProgram: RenderProgram, endProgram: RenderProgram);
    getUnitReversed(): RenderLinearRange;
    withOffset(offset: number): RenderLinearRange;
    static getGradientLinearRanges(min: number, max: number, offset: number, extend: RenderExtend, stops: RenderGradientStop[]): RenderLinearRange[];
}
//# sourceMappingURL=RenderLinearRange.d.ts.map