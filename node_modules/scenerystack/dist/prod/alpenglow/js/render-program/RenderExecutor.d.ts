/**
 * Executes stack-based evaluation of a RenderProgram
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import Vector4 from '../../../dot/js/Vector4.js';
import { RenderInstruction, RenderInstructionLocation } from './RenderInstruction.js';
import type { RenderEvaluator, RenderProgram } from './RenderProgram.js';
import type { RenderEvaluationContext } from './RenderEvaluationContext.js';
export declare class RenderExecutor {
    private stack;
    private indexStack;
    private instructions;
    private instructionIndex;
    private isDone;
    readonly evaluator: RenderEvaluator;
    constructor();
    loadRenderProgram(renderProgram: RenderProgram): void;
    loadInstructions(instructions: RenderInstruction[]): void;
    reset(): void;
    execute(context: RenderEvaluationContext, output: Vector4): Vector4;
    jump(location: RenderInstructionLocation): void;
    call(location: RenderInstructionLocation): void;
    return(): void;
    exit(): void;
    private updateIndices;
}
//# sourceMappingURL=RenderExecutor.d.ts.map