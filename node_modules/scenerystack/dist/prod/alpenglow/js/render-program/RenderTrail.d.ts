/**
 * Represents an ancestor-to-descendant walk through a RenderProgram tree.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import type { RenderProgram } from './RenderProgram.js';
export declare class RenderTrail {
    readonly program: RenderProgram;
    readonly indices: number[];
    constructor(program: RenderProgram, indices: number[]);
    equals(other: RenderTrail): boolean;
    compare(other: RenderTrail): number;
    static closureCompare(a: RenderTrail, b: RenderTrail): number;
}
//# sourceMappingURL=RenderTrail.d.ts.map