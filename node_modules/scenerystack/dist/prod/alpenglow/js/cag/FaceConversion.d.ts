/**
 * Multiple methods of conversion from RationalFaces to RenderableFaces.
 *
 * They mostly differ on whether they combine faces with equivalent RenderPrograms, WHICH cases they do so, and
 * whether they output polygonal or unsorted-edge formats (PolygonalFace/EdgedFace).
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import Matrix3 from '../../../dot/js/Matrix3.js';
import { RationalFace } from './RationalFace.js';
import { ClippableFaceAccumulator } from './ClippableFace.js';
import { RenderableFace } from '../raster/RenderableFace.js';
export declare class FaceConversion {
    static toSimpleRenderableFaces(faces: RationalFace[], fromIntegerMatrix: Matrix3, accumulator: ClippableFaceAccumulator): RenderableFace[];
    static toFullyCombinedRenderableFaces(faces: RationalFace[], fromIntegerMatrix: Matrix3, accumulator: ClippableFaceAccumulator): RenderableFace[];
    static toSimplifyingCombinedRenderableFaces(faces: RationalFace[], fromIntegerMatrix: Matrix3, accumulator: ClippableFaceAccumulator): RenderableFace[];
    /**
     * Combines faces that have equivalent RenderPrograms IFF they border each other (leaving separate programs with
     * equivalent RenderPrograms separate if they don't border). It will also remove edges that border between faces
     * that we combine, and will connect edges to keep things polygonal!
     */
    static toTracedRenderableFaces(faces: RationalFace[], fromIntegerMatrix: Matrix3, accumulator: ClippableFaceAccumulator): RenderableFace[];
}
//# sourceMappingURL=FaceConversion.d.ts.map