/**
 * Merges two sorted arrays into a single sorted array.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../../globals.js';
import type { BufferArraySlot } from '../../compute/BufferArraySlot.js';
import { mainMergeWGSLOptions } from '../../wgsl/gpu/mainMergeWGSL.js';
import type { PipelineBlueprintOptions } from '../../compute/PipelineBlueprint.js';
import { DirectModule } from '../../compute/DirectModule.js';
export type MergeModuleOptions<T> = {
    inputA: BufferArraySlot<T>;
    inputB: BufferArraySlot<T>;
    output: BufferArraySlot<T>;
} & mainMergeWGSLOptions<T> & PipelineBlueprintOptions;
export declare const MERGE_MODULE_DEFAULTS: {
    readonly log: false;
};
export declare class MergeModule<T> extends DirectModule<number> {
    readonly inputA: BufferArraySlot<T>;
    readonly inputB: BufferArraySlot<T>;
    readonly output: BufferArraySlot<T>;
    constructor(providedOptions: MergeModuleOptions<T>);
}
//# sourceMappingURL=MergeModule.d.ts.map