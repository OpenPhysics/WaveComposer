/**
 * Meant for reduction on u32/i32 values (could be generalized to things that can be represented with multiple atomic
 * values, haven't run into that yet).
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../../globals.js';
import type { BufferArraySlot } from '../../compute/BufferArraySlot.js';
import { BufferSlot } from '../../compute/BufferSlot.js';
import { mainReduceAtomicWGSLOptions } from '../../wgsl/gpu/mainReduceAtomicWGSL.js';
import type { PipelineBlueprintOptions } from '../../compute/PipelineBlueprint.js';
import { DirectModule } from '../../compute/DirectModule.js';
export type MainReduceAtomicModuleOptions<T> = {
    input: BufferArraySlot<T>;
    output: BufferSlot<T>;
} & mainReduceAtomicWGSLOptions<T> & PipelineBlueprintOptions;
export declare const MAIN_REDUCE_ATOMIC_MODULE_DEFAULTS: {
    readonly loadReducedOptions: {
        readonly inputAccessOrder: "striped";
    };
    readonly reduceOptions: {
        readonly convergent: true;
    };
    readonly log: false;
};
export declare class MainReduceAtomicModule<T> extends DirectModule<number> {
    readonly input: BufferArraySlot<T>;
    readonly output: BufferSlot<T>;
    constructor(providedOptions: MainReduceAtomicModuleOptions<T>);
}
//# sourceMappingURL=MainReduceAtomicModule.d.ts.map