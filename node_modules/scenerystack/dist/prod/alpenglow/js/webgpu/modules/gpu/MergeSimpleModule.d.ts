/**
 * Merges two sorted arrays into a single sorted array (with a simpler algorithm than MergeModule).
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../../globals.js';
import type { BufferArraySlot } from '../../compute/BufferArraySlot.js';
import { mainMergeSimpleWGSLOptions } from '../../wgsl/gpu/mainMergeSimpleWGSL.js';
import type { PipelineBlueprintOptions } from '../../compute/PipelineBlueprint.js';
import { DirectModule } from '../../compute/DirectModule.js';
export type MergeSimpleModuleOptions<T> = {
    inputA: BufferArraySlot<T>;
    inputB: BufferArraySlot<T>;
    output: BufferArraySlot<T>;
} & mainMergeSimpleWGSLOptions<T> & PipelineBlueprintOptions;
export declare const MERGE_SIMPLE_MODULE_DEFAULTS: {
    readonly log: false;
};
export declare class MergeSimpleModule<T> extends DirectModule<number> {
    readonly inputA: BufferArraySlot<T>;
    readonly inputB: BufferArraySlot<T>;
    readonly output: BufferArraySlot<T>;
    constructor(providedOptions: MergeSimpleModuleOptions<T>);
}
//# sourceMappingURL=MergeSimpleModule.d.ts.map