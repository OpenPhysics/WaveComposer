// Copyright 2024-2025, University of Colorado Boulder
/**
 * Applies a histogram operation to an array.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../../globals.js';
import { combineOptions } from '../../../../../phet-core/js/optionize.js';
import { MAIN_HISTOGRAM_DEFAULTS, mainHistogramWGSL } from '../../wgsl/gpu/mainHistogramWGSL.js';
import { DIRECT_MODULE_DEFAULTS, DirectModule } from '../../compute/DirectModule.js';
export const HISTOGRAM_MODULE_DEFAULTS = {
    // eslint-disable-next-line phet/no-object-spread-on-non-literals
    ...DIRECT_MODULE_DEFAULTS,
    // eslint-disable-next-line phet/no-object-spread-on-non-literals
    ...MAIN_HISTOGRAM_DEFAULTS
};
// inputSize: number
export class HistogramModule extends DirectModule {
    constructor(providedOptions) {
        const options = combineOptions({
            main: mainHistogramWGSL(providedOptions),
            setDispatchSize: (dispatchSize, inputSize) => {
                dispatchSize.x = Math.ceil(inputSize / (providedOptions.workgroupSize * providedOptions.grainSize));
            }
        }, providedOptions);
        super(options);
        this.input = options.input;
        this.output = options.output;
    }
}
//# sourceMappingURL=HistogramModule.js.map