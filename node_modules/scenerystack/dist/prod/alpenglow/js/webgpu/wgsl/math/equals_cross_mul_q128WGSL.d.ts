/**
 * Checks whether two rational numbers are equal (by using cross-multiplication).
 *
 * IMPORTANT NOTE: this only works if we have the bits to spare (where numerator * denominator does NOT overflow)
 * to avoid reduction. Reduction would also work.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../../globals.js';
import { WGSLExpression, WGSLExpressionBool } from '../WGSLString.js';
export declare const equals_cross_mul_q128WGSL: (a: WGSLExpression, b: WGSLExpression) => WGSLExpressionBool;
//# sourceMappingURL=equals_cross_mul_q128WGSL.d.ts.map