/**
 * Represents an edge from a RasterChunk
 *
 * Used for the raster-clip input, and the output for reducible edges (that will be fed back in)
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
import Vector2 from '../../../../dot/js/Vector2.js';
import type { ByteEncoder } from '../../webgpu/compute/ByteEncoder.js';
export declare class RasterEdge {
    readonly chunkIndex: number;
    readonly isFirstEdge: boolean;
    readonly isLastEdge: boolean;
    readonly startPoint: Vector2;
    readonly endPoint: Vector2;
    constructor(chunkIndex: number, isFirstEdge: boolean, isLastEdge: boolean, startPoint: Vector2, endPoint: Vector2);
    toString(): string;
    static readonly ENCODING_BYTE_LENGTH: number;
    writeEncoding(encoder: ByteEncoder): void;
    static readEncoding(arrayBuffer: ArrayBuffer, byteOffset: number): RasterEdge;
    static fromArrayBuffer(arrayBuffer: ArrayBuffer): RasterEdge[];
    static readonly INDETERMINATE: RasterEdge;
}
//# sourceMappingURL=RasterEdge.d.ts.map