/**
 * Takes the input reductions and computes the inclusive prefix sum (scan) into it, in a form that can be used for
 * computing the exclusive prefix sum (zeros the last element). Outputs the reduction of the entire input into the
 * output reduces.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
import type { ParallelStorageArray } from '../ParallelStorageArray.js';
import { RasterSplitReduceData } from './RasterSplitReduceData.js';
export declare class ParallelRasterSplitReduce {
    static dispatch(workgroupSize: number, numReduces: number, inputReduces: ParallelStorageArray<RasterSplitReduceData>, // will process into scanned form
    outputReduces: ParallelStorageArray<RasterSplitReduceData>): Promise<void>;
}
//# sourceMappingURL=ParallelRasterSplitReduce.d.ts.map