/**
 * Represents the clipped state of a RasterEdge. For the binary version, there will be two of these edge clips per
 * input edge.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
import Vector2 from '../../../../dot/js/Vector2.js';
import type { ByteEncoder } from '../../webgpu/compute/ByteEncoder.js';
export declare class RasterEdgeClip {
    readonly clippedChunkIndex: number;
    readonly point0: Vector2;
    readonly point1: Vector2;
    readonly point2: Vector2;
    readonly point3: Vector2;
    readonly isFirstEdge: boolean;
    readonly isLastEdge: boolean;
    constructor(clippedChunkIndex: number, point0: Vector2, point1: Vector2, point2: Vector2, point3: Vector2, isFirstEdge: boolean, isLastEdge: boolean);
    getCount(): number;
    getArea(): number;
    toString(): string;
    static readonly ENCODING_BYTE_LENGTH: number;
    writeEncoding(encoder: ByteEncoder): void;
    static readEncoding(arrayBuffer: ArrayBuffer, byteOffset: number): RasterEdgeClip;
    static fromArrayBuffer(arrayBuffer: ArrayBuffer): RasterEdgeClip[];
    static readonly INDETERMINATE: RasterEdgeClip;
}
//# sourceMappingURL=RasterEdgeClip.d.ts.map