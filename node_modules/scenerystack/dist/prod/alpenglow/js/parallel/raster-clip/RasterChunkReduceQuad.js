// Copyright 2023-2025, University of Colorado Boulder
/**
 * Represents an "applied interval" of reduce data, for both the min and max binary clips.
 *
 * In our segmented reduction, at each level we need to track intervals with the "fragments" of chunks' edges that
 * have not been completed yet.
 *
 * "left" means "the reduction of all values for the chunk index at the start of this interval"
 * "right" means "the reduction of all values for the chunk index at the end of this interval"
 *
 * Sometimes the left/right are equal (IFF they have the same chunk index).
 *
 * Each reduced data notes whether it contains the first/last edge of the chunk it references. Once we combine reduces
 * so that it contains both the first and last edge, we can "apply" it, moving the reduced data into the chunk.
 *
 * TODO: produce general documentation on this whole process.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
import _ from 'lodash';
import { RasterChunkReduceData } from './RasterChunkReduceData.js';
export class RasterChunkReduceQuad {
    constructor(leftMin, leftMax, rightMin, rightMax) {
        this.leftMin = leftMin;
        this.leftMax = leftMax;
        this.rightMin = rightMin;
        this.rightMax = rightMax;
    }
    static combine(a, b) {
        return new RasterChunkReduceQuad(
        // We need separate logic for the "left" combine, due to the combine "taking" the right side
        a.leftMin.clippedChunkIndex === b.leftMin.clippedChunkIndex ? RasterChunkReduceData.combine(a.leftMin, b.leftMin) : a.leftMin, a.leftMax.clippedChunkIndex === b.leftMax.clippedChunkIndex ? RasterChunkReduceData.combine(a.leftMax, b.leftMax) : a.leftMax, RasterChunkReduceData.combine(a.rightMin, b.rightMin), RasterChunkReduceData.combine(a.rightMax, b.rightMax));
    }
    toStrings() {
        return [
            `leftMin: ${this.leftMin.toString()}`,
            `rightMin: ${this.rightMin.toString()}`,
            `leftMax: ${this.leftMax.toString()}`,
            `rightMax: ${this.rightMax.toString()}`
        ];
    }
    writeEncoding(encoder) {
        this.leftMin.writeEncoding(encoder);
        this.leftMax.writeEncoding(encoder);
        this.rightMin.writeEncoding(encoder);
        this.rightMax.writeEncoding(encoder);
    }
    static readEncoding(arrayBuffer, byteOffset) {
        return new RasterChunkReduceQuad(RasterChunkReduceData.readEncoding(arrayBuffer, byteOffset), RasterChunkReduceData.readEncoding(arrayBuffer, byteOffset + RasterChunkReduceData.ENCODING_BYTE_LENGTH), RasterChunkReduceData.readEncoding(arrayBuffer, byteOffset + 2 * RasterChunkReduceData.ENCODING_BYTE_LENGTH), RasterChunkReduceData.readEncoding(arrayBuffer, byteOffset + 3 * RasterChunkReduceData.ENCODING_BYTE_LENGTH));
    }
    static fromArrayBuffer(arrayBuffer) {
        ;
        return _.range(0, arrayBuffer.byteLength / RasterChunkReduceQuad.ENCODING_BYTE_LENGTH).map(i => {
            return RasterChunkReduceQuad.readEncoding(arrayBuffer, i * RasterChunkReduceQuad.ENCODING_BYTE_LENGTH);
        });
    }
}
RasterChunkReduceQuad.ENCODING_BYTE_LENGTH = 4 * RasterChunkReduceData.ENCODING_BYTE_LENGTH;
RasterChunkReduceQuad.INDETERMINATE = new RasterChunkReduceQuad(RasterChunkReduceData.INDETERMINATE, RasterChunkReduceData.INDETERMINATE, RasterChunkReduceData.INDETERMINATE, RasterChunkReduceData.INDETERMINATE);
RasterChunkReduceQuad.OUT_OF_RANGE = new RasterChunkReduceQuad(RasterChunkReduceData.OUT_OF_RANGE, RasterChunkReduceData.OUT_OF_RANGE, RasterChunkReduceData.OUT_OF_RANGE, RasterChunkReduceData.OUT_OF_RANGE);
//# sourceMappingURL=RasterChunkReduceQuad.js.map