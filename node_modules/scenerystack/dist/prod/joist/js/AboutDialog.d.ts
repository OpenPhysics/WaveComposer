import '../../globals.js';
import type { TReadOnlyProperty } from '../../axon/js/TReadOnlyProperty.js';
import { type EmptySelfOptions } from '../../phet-core/js/optionize.js';
import Dialog, { type DialogOptions } from '../../sun/js/Dialog.js';
import { type CreditsData } from './CreditsNode.js';
type SelfOptions = EmptySelfOptions;
export type AboutDialogOptions = SelfOptions & DialogOptions;
export default class AboutDialog extends Dialog {
    private readonly updateStepListener;
    private readonly updateVisibilityListener;
    /**
     * @param nameStringProperty - name of the simulation
     * @param version - version of the simulation
     * @param credits - credits for the simulation
     * @param [providedOptions]
     */
    constructor(nameStringProperty: TReadOnlyProperty<string>, version: string, credits: CreditsData, providedOptions?: AboutDialogOptions);
    /**
     * When the dialog is shown, add update listeners.
     */
    show(): void;
    /**
     * When the dialog is hidden, remove update listeners.
     */
    hide(): void;
}
export {};
//# sourceMappingURL=AboutDialog.d.ts.map