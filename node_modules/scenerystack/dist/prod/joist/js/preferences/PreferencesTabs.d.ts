import '../../../globals.js';
import type TProperty from '../../../axon/js/TProperty.js';
import type { TReadOnlyProperty } from '../../../axon/js/TReadOnlyProperty.js';
import type PickRequired from '../../../phet-core/js/types/PickRequired.js';
import HBox, { type HBoxOptions } from '../../../scenery/js/layout/nodes/HBox.js';
import PreferencesType from './PreferencesType.js';
export type PreferencesTabsOptions = HBoxOptions & PickRequired<HBoxOptions, 'tandem'>;
declare class PreferencesTabs extends HBox {
    private selectedButton;
    private readonly selectedPanelProperty;
    private readonly content;
    constructor(supportedTabs: PreferencesType[], selectedPanelProperty: TProperty<PreferencesType>, providedOptions: PreferencesTabsOptions);
    /**
     * Returns the visibleProperty for the Tab associated with the PreferencesType. PreferencesTabs need to be hidden
     * if the Tab becomes invisible (mostly needed for PhET-iO).
     */
    getTabVisibleProperty(preferencesType: PreferencesType): TReadOnlyProperty<boolean>;
    /**
     * Move focus to the selected tab. Useful when the Preferences dialog is opened.
     */
    focusSelectedTab(): void;
}
export default PreferencesTabs;
//# sourceMappingURL=PreferencesTabs.d.ts.map