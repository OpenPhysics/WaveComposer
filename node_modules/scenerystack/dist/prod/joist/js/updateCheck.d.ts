/**
 * A singleton type/object for handling checking whether our simulation is up-to-date, or whether there is an
 * updated version. See https://github.com/phetsims/joist/issues/189
 *
 * It exposes its current state (for UIs to hook into), and a check() function used to start checking the version.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../globals.js';
import '../../chipper/js/browser/initialize-globals.js';
import EnumerationProperty from '../../axon/js/EnumerationProperty.js';
import SimVersion from '../../perennial-alias/js/browser-and-node/SimVersion.js';
import UpdateState from './UpdateState.js';
declare class UpdateCheck {
    readonly stateProperty: EnumerationProperty<UpdateState>;
    latestVersion: SimVersion | null;
    readonly ourVersion: SimVersion;
    private readonly timeoutCallback;
    readonly areUpdatesChecked: boolean;
    updateURL: string;
    private timeoutId;
    constructor();
    private clearTimeout;
    private setTimeout;
    resetTimeout(): void;
    private timeout;
    /**
     * Kicks off the version checking request (if able), resulting in state changes.
     */
    check(): void;
}
declare const updateCheck: UpdateCheck;
export default updateCheck;
//# sourceMappingURL=updateCheck.d.ts.map