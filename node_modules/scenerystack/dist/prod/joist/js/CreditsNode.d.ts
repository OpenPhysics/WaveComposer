import '../../globals.js';
import PhetFont from '../../scenery-phet/js/PhetFont.js';
import VBox, { type VBoxOptions } from '../../scenery/js/layout/nodes/VBox.js';
type SelfOptions = {
    titleFont?: PhetFont;
    textFont?: PhetFont;
};
type CreditsNodeOptions = SelfOptions & VBoxOptions;
export type CreditsData = {
    leadDesign?: string;
    softwareDevelopment?: string;
    team?: string;
    contributors?: string;
    qualityAssurance?: string;
    graphicArts?: string;
    soundDesign?: string;
    thanks?: string;
};
export default class CreditsNode extends VBox {
    private readonly disposeCreditsNode;
    constructor(credits: CreditsData, options: CreditsNodeOptions);
    dispose(): void;
}
export {};
//# sourceMappingURL=CreditsNode.d.ts.map