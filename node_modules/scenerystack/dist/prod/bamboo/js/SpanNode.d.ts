/**
 * SpanNode shows a double-headed arrow pointing to parallel bars, and a text label. It is shown under a chart to
 * indicate the distance between gridlines.
 *
 * @author Sam Reid (PhET Interactive Simulations)
 */
import '../../globals.js';
import Orientation from '../../phet-core/js/Orientation.js';
import StrictOmit from '../../phet-core/js/types/StrictOmit.js';
import { ArrowNodeOptions } from '../../scenery-phet/js/ArrowNode.js';
import FlowBox, { FlowBoxOptions } from '../../scenery/js/layout/nodes/FlowBox.js';
import Node from '../../scenery/js/nodes/Node.js';
import Color from '../../scenery/js/util/Color.js';
import ChartTransform from './ChartTransform.js';
type SelfOptions = {
    color?: string | Color;
    outerLineLength?: number;
    arrowNodeOptions?: StrictOmit<ArrowNodeOptions, 'stroke'>;
};
export type SpanNodeOptions = SelfOptions & StrictOmit<FlowBoxOptions, 'children' | 'orientation'>;
declare class SpanNode extends FlowBox {
    private chartTransform;
    private readonly axisOrientation;
    private delta;
    private readonly labelNode;
    private readonly color;
    private readonly outerLineLength;
    private viewWidth;
    private readonly arrowNodeOptions;
    private readonly disposeSpanNode;
    /**
     * @param chartTransform
     * @param axisOrientation
     * @param delta - in model coordinates
     * @param labelNode
     * @param [providedOptions]
     */
    constructor(chartTransform: ChartTransform, axisOrientation: Orientation, delta: number, labelNode: Node, providedOptions?: SpanNodeOptions);
    /**
     * Sets delta and updates.
     * @param delta - in model coordinates
     */
    setDelta(delta: number): void;
    private update;
    dispose(): void;
}
export default SpanNode;
//# sourceMappingURL=SpanNode.d.ts.map