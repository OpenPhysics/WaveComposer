/**
 * Shows a plot with arrows for numerical data (where the x-value of the bar is a number).
 *
 * @author Jesse Greenberg (PhET Interactive Simulations)
 */
import '../../globals.js';
import Vector2 from '../../dot/js/Vector2.js';
import { ArrowNodeOptions } from '../../scenery-phet/js/ArrowNode.js';
import Node, { NodeOptions } from '../../scenery/js/nodes/Node.js';
import { PaintableOptions } from '../../scenery/js/nodes/Paintable.js';
import ChartTransform from './ChartTransform.js';
type SelfOptions = {
    arrowNodeOptions?: ArrowNodeOptions;
    pointToPaintableFields?: (point: Vector2) => PaintableOptions;
};
export type UpDownArrowPlotOptions = SelfOptions & NodeOptions;
declare class UpDownArrowPlot extends Node {
    private chartTransform;
    dataSet: Vector2[];
    private readonly pointToPaintableFields;
    private arrowNodes;
    private readonly arrowNodeOptions;
    private readonly disposeUpDownArrowPLot;
    constructor(chartTransform: ChartTransform, dataSet: Vector2[], providedOptions?: UpDownArrowPlotOptions);
    setDataSet(dataSet: Vector2[]): void;
    /**
     * Redraw the plot. Called automatically if you update the dataSet with setDataSet(). But you can call this
     * yourself if you want to set the dataSet directly and then update later (presumably for performance).
     */
    update(): void;
    dispose(): void;
}
export default UpDownArrowPlot;
//# sourceMappingURL=UpDownArrowPlot.d.ts.map