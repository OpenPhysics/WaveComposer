/**
 * MenuItem is an item in the PhetMenu.
 *
 * @author Michael Kauzmann (PhET Interactive Simulations)
 */
import '../../globals.js';
import type TProperty from '../../axon/js/TProperty.js';
import type { TReadOnlyProperty } from '../../axon/js/TReadOnlyProperty.js';
import { type VoicingOptions } from '../../scenery/js/accessibility/voicing/Voicing.js';
import type SceneryEvent from '../../scenery/js/input/SceneryEvent.js';
import Node, { type NodeOptions } from '../../scenery/js/nodes/Node.js';
import type TPaint from '../../scenery/js/util/TPaint.js';
type SelfOptions = {
    separatorBefore?: boolean;
    checkedProperty?: TProperty<boolean> | null;
    textFill?: TPaint;
};
type ParentOptions = VoicingOptions & NodeOptions;
export type MenuItemOptions = SelfOptions & ParentOptions;
declare const MenuItem_base: typeof Node & import("../../phet-core.js").Constructor<import("../../scenery/js/accessibility/voicing/Voicing.js").TVoicing<Node>> & import("../../phet-core.js").Constructor<import("../../scenery/js/layout/WidthSizable.js").TWidthSizable>;
export default class MenuItem extends MenuItem_base {
    readonly present: boolean;
    readonly separatorBefore: boolean;
    private readonly disposeMenuItem;
    /**
     * @param closeCallback - called when firing the menu item, most likely this should close the PhetMenu.
     * @param labelStringProperty - label for the menu item
     * @param callback - called when the menu item is selected
     * @param present - see present field
     * @param shouldBeHiddenWhenLinksAreNotAllowed
     * @param [providedOptions]
     */
    constructor(closeCallback: (event: SceneryEvent | null) => void, labelStringProperty: TReadOnlyProperty<string>, callback: (event: SceneryEvent | null) => void, present: boolean, shouldBeHiddenWhenLinksAreNotAllowed: boolean, providedOptions?: MenuItemOptions);
    dispose(): void;
}
export {};
//# sourceMappingURL=MenuItem.d.ts.map