// Copyright 2014-2025, University of Colorado Boulder
/**
 * A DerivedProperty that maps ToggleButtonModel states to the states needed by the button view.
 *
 * @author Sam Reid (PhET Interactive Simulations)
 */
import '../../../globals.js';
import { DerivedProperty2 } from '../../../axon/js/DerivedProperty.js';
import ButtonInteractionState from './ButtonInteractionState.js';
export default class ToggleButtonInteractionStateProperty extends DerivedProperty2 {
    constructor(toggleButtonModel) {
        super([toggleButtonModel.isOverOrFocusedProperty, toggleButtonModel.looksPressedProperty], (overOrFocused, looksPressed) => {
            return overOrFocused && !looksPressed ? ButtonInteractionState.OVER :
                looksPressed ? ButtonInteractionState.PRESSED :
                    ButtonInteractionState.IDLE;
        }, { valueType: ButtonInteractionState });
    }
}
//# sourceMappingURL=ToggleButtonInteractionStateProperty.js.map