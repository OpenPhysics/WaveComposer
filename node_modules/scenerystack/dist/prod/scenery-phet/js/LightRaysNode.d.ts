/**
 * Light rays that indicate brightness of a light source such as a bulb.
 *
 * @author Chris Malley (PixelZoom, Inc.)
 */
import '../../globals.js';
import Path, { PathOptions } from '../../scenery/js/nodes/Path.js';
type SelfOptions = {
    minRays?: number;
    maxRays?: number;
    minRayLength?: number;
    maxRayLength?: number;
    longRayLineWidth?: number;
    mediumRayLineWidth?: number;
    shortRayLineWidth?: number;
};
export type LightRaysNodeOptions = SelfOptions & PathOptions;
export default class LightRaysNode extends Path {
    private readonly bulbRadius;
    private readonly minRays;
    private readonly maxRays;
    private readonly minRayLength;
    private readonly maxRayLength;
    private readonly longRayLineWidth;
    private readonly mediumRayLineWidth;
    private readonly shortRayLineWidth;
    constructor(bulbRadius: number, provideOptions?: LightRaysNodeOptions);
    /**
     * Sets the brightness, which updates the number and length of light rays.
     * @param brightness -a value in the range [0,1]
     */
    setBrightness(brightness: number): void;
}
export {};
//# sourceMappingURL=LightRaysNode.d.ts.map