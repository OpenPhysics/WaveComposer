/**
 * Provides a 2-way mapping between wavelength and Color.
 * The mapping is performed using a color lookup table.
 *
 * Note that the sRGB colorspace is not capable of representing all visible colors.
 * So in converting visible wavelengths to Color, it is possible to lose some color information.
 *
 * @author Chris Malley (PixelZoom, Inc.)
 */
import '../../globals.js';
import Color from '../../scenery/js/util/Color.js';
type TColorSubset = Color | string | null;
export type WavelengthToColorOptions = {
    irColor?: TColorSubset;
    uvColor?: TColorSubset;
    reduceIntensityAtExtrema?: boolean;
};
declare const VisibleColor: {
    MIN_WAVELENGTH: number;
    MAX_WAVELENGTH: number;
    MIN_FREQUENCY: number;
    MAX_FREQUENCY: number;
    SPEED_OF_LIGHT: number;
    WHITE_WAVELENGTH: number;
    /**
     * Converts a wavelength (in nm, rounded to the nearest integer) to a visible color.
     */
    wavelengthToColor: (wavelength: number, providedOptions?: WavelengthToColorOptions) => Color;
    /**
     * Converts a frequency (in Hz) to a visible color.
     */
    frequencyToColor: (frequency: number, providedOptions?: WavelengthToColorOptions) => Color;
    /**
     * Converts a Color to its corresponding wavelength. Relies on a color lookup table that is initialized the first
     * time that this method is called.  Color lookup is based on RGB component value; the alpha value is ignored.
     */
    colorToWavelength: (color: TColorSubset, reduceIntensityAtExtrema?: boolean) => number;
    /**
     * Is the specified wavelength in the visible spectrum?
     */
    isVisibleWavelength(wavelength: number): boolean;
    /**
     * Is the specified wavelength in the IR spectrum?
     */
    isIRWavelength(wavelength: number): boolean;
    /**
     * Is the specified wavelength in the UV spectrum?
     */
    isUVWavelength(wavelength: number): boolean;
};
export default VisibleColor;
//# sourceMappingURL=VisibleColor.d.ts.map