/**
 * Stopwatch is the model for the stopwatch. It is responsible for time, position, and visibility.
 *
 * @author Chris Malley (PixelZoom, Inc.)
 */
import '../../globals.js';
import NumberProperty, { NumberPropertyOptions } from '../../axon/js/NumberProperty.js';
import Property from '../../axon/js/Property.js';
import Range from '../../dot/js/Range.js';
import Vector2 from '../../dot/js/Vector2.js';
import StrictOmit from '../../phet-core/js/types/StrictOmit.js';
import PhetioObject, { PhetioObjectOptions } from '../../tandem/js/PhetioObject.js';
type SelfOptions = {
    position?: Vector2;
    isVisible?: boolean;
    timePropertyOptions?: StrictOmit<NumberPropertyOptions, 'tandem'>;
};
export type StopwatchOptions = SelfOptions & PhetioObjectOptions;
export default class Stopwatch extends PhetioObject {
    readonly positionProperty: Property<Vector2>;
    readonly isVisibleProperty: Property<boolean>;
    readonly isRunningProperty: Property<boolean>;
    readonly timeProperty: NumberProperty;
    private readonly disposeStopwatch;
    static readonly ZERO_TO_ALMOST_SIXTY: Range;
    constructor(providedOptions?: StopwatchOptions);
    dispose(): void;
    reset(): void;
    /**
     * Steps the stopwatch.
     * @param dt - time delta, in units as specified by the client
     */
    step(dt: number): void;
    /**
     * Similar to step() but sets the time to a specific value.
     * @param t
     */
    setTime(t: number): void;
}
export {};
//# sourceMappingURL=Stopwatch.d.ts.map