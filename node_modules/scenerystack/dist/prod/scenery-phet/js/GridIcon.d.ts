/**
 * GridIcon is the icon for an NxN grid of square cells.
 *
 * @author Chris Malley (PixelZoom, Inc.)
 */
import '../../globals.js';
import Path, { PathOptions } from '../../scenery/js/nodes/Path.js';
type SelfOptions = {
    size?: number;
    numberOfRows?: number;
};
export type GridIconOptions = SelfOptions & PathOptions;
export default class GridIcon extends Path {
    constructor(providedOptions?: GridIconOptions);
}
export {};
//# sourceMappingURL=GridIcon.d.ts.map