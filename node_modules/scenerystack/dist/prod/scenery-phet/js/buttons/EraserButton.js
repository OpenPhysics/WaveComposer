// Copyright 2014-2025, University of Colorado Boulder
/**
 * Button with an eraser icon.
 *
 * @author John Blanco
 */
import { string_scenery_phet_a11y_eraserButton_accessibleName_StringProperty } from '../strings/a11y/eraserButton/accessibleName.js';
import '../../../globals.js';
import '../../../chipper/js/browser/initialize-globals.js';
import optionize from '../../../phet-core/js/optionize.js';
import Image from '../../../scenery/js/nodes/Image.js';
import RectangularPushButton from '../../../sun/js/buttons/RectangularPushButton.js';
import sharedSoundPlayers from '../../../tambo/js/sharedSoundPlayers.js';
import eraser_svg from '../../images/eraser_svg.js';
import PhetColorScheme from '../PhetColorScheme.js';
export default class EraserButton extends RectangularPushButton {
    constructor(providedOptions) {
        const options = optionize()({
            // SelfOptions
            iconWidth: 20,
            // RectangularPushButtonOptions
            baseColor: PhetColorScheme.BUTTON_YELLOW,
            // pdom
            accessibleName: string_scenery_phet_a11y_eraserButton_accessibleName_StringProperty,
            // sound
            soundPlayer: sharedSoundPlayers.get('erase')
        }, providedOptions);
        // eraser icon
        options.content = new Image(eraser_svg);
        options.content.scale(options.iconWidth / options.content.width);
        super(options);
        // support for binder documentation, stripped out in builds and only runs when ?binder is specified
        ;
    }
}
//# sourceMappingURL=EraserButton.js.map