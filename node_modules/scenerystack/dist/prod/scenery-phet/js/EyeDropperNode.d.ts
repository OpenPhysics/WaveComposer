/**
 * Eye dropper, with a button for dispensing whatever is in the dropper.
 *
 * @author Chris Malley (PixelZoom, Inc.)
 */
import '../../globals.js';
import '../../chipper/js/browser/initialize-globals.js';
import Property from '../../axon/js/Property.js';
import StrictOmit from '../../phet-core/js/types/StrictOmit.js';
import Node, { NodeOptions } from '../../scenery/js/nodes/Node.js';
import TPaint from '../../scenery/js/util/TPaint.js';
import { RoundMomentaryButtonOptions } from '../../sun/js/buttons/RoundMomentaryButton.js';
type SelfOptions = {
    isDispensingProperty?: Property<boolean>;
    isEmptyProperty?: Property<boolean>;
    fluidColor?: TPaint;
    buttonOptions?: RoundMomentaryButtonOptions;
};
export type EyeDropperNodeOptions = SelfOptions & StrictOmit<NodeOptions, 'children'>;
export default class EyeDropperNode extends Node {
    readonly isDispensingProperty: Property<boolean>;
    readonly isEmptyProperty: Property<boolean>;
    readonly button: Node;
    private readonly fluidNode;
    private readonly disposeEyeDropperNode;
    static readonly TIP_WIDTH = 15;
    static readonly TIP_HEIGHT = 4;
    static readonly GLASS_WIDTH = 46;
    static readonly GLASS_MIN_Y = -124;
    static readonly GLASS_MAX_Y = -18;
    constructor(provideOptions?: EyeDropperNodeOptions);
    dispose(): void;
    /**
     * Gets the color of the fluid in the dropper.
     */
    getFluidColor(): TPaint;
    /**
     * Sets the color of the fluid in the dropper.
     */
    setFluidColor(color: TPaint): void;
    get fluidColor(): TPaint;
    set fluidColor(value: TPaint);
}
export {};
//# sourceMappingURL=EyeDropperNode.d.ts.map