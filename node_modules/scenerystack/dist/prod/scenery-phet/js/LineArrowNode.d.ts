/**
 * An arrow that is composed of 3 line segments: one for the tail, and 2 for a V-shaped head
 *
 * @author Chris Malley (PixelZoom, Inc.)
 */
import '../../globals.js';
import '../../chipper/js/browser/initialize-globals.js';
import { LineCap, LineJoin } from '../../kite/js/util/LineStyles.js';
import StrictOmit from '../../phet-core/js/types/StrictOmit.js';
import Node, { NodeOptions } from '../../scenery/js/nodes/Node.js';
import TColor from '../../scenery/js/util/TColor.js';
type SelfOptions = {
    stroke?: TColor;
    lineJoin?: LineJoin;
    lineCap?: LineCap;
    headHeight?: number;
    headWidth?: number;
    headLineWidth?: number;
    tailLineWidth?: number;
    tailLineDash?: number[];
};
export type LineArrowNodeOptions = SelfOptions & StrictOmit<NodeOptions, 'children'>;
export default class LineArrowNode extends Node {
    private readonly headWidth;
    private readonly headHeight;
    private readonly tailNode;
    private readonly headNode;
    constructor(tailX: number, tailY: number, tipX: number, tipY: number, providedOptions?: LineArrowNodeOptions);
    /**
     * Set the tail and tip positions to update the arrow shape.
     */
    setTailAndTip(tailX: number, tailY: number, tipX: number, tipY: number): void;
}
export {};
//# sourceMappingURL=LineArrowNode.d.ts.map