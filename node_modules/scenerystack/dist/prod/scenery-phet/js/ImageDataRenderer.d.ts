/**
 * Utility type for rendering to a canvas via putImageData.
 *
 * @author Sam Reid (PhET Interactive Simulations)
 */
import '../../globals.js';
declare class ImageDataRenderer {
    readonly canvas: HTMLCanvasElement;
    private readonly context;
    private readonly imageData;
    readonly data: Uint8ClampedArray;
    constructor(width: number, height: number);
    /**
     * Paints image data onto the canvas.
     */
    putImageData(x?: number, y?: number): void;
}
export default ImageDataRenderer;
//# sourceMappingURL=ImageDataRenderer.d.ts.map