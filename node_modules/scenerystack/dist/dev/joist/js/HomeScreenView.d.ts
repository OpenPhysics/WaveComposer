import '../../globals.js';
import type { TReadOnlyProperty } from '../../axon/js/TReadOnlyProperty.js';
import Bounds2 from '../../dot/js/Bounds2.js';
import type PickRequired from '../../phet-core/js/types/PickRequired.js';
import Node from '../../scenery/js/nodes/Node.js';
import HomeScreenButton from './HomeScreenButton.js';
import type HomeScreenModel from './HomeScreenModel.js';
import ScreenView, { type ScreenViewOptions } from './ScreenView.js';
type SelfOptions = {
    warningNode?: Node | null;
};
type HomeScreenViewOptions = SelfOptions & PickRequired<ScreenViewOptions, 'tandem'>;
declare class HomeScreenView extends ScreenView {
    private homeScreenScreenSummaryIntroProperty;
    private selectedScreenProperty;
    screenButtons: HomeScreenButton[];
    static readonly LAYOUT_BOUNDS: Bounds2;
    static readonly TITLE_FONT_FAMILY = "Century Gothic, Futura";
    /**
     * @param simNameProperty - the internationalized text for the sim name
     * @param model
     * @param [providedOptions]
     */
    constructor(simNameProperty: TReadOnlyProperty<string>, model: HomeScreenModel, providedOptions?: HomeScreenViewOptions);
    /**
     * For a11y, highlight the currently selected screen button
     */
    focusHighlightedScreenButton(): void;
    /**
     * To support voicing.
     */
    getVoicingOverviewContent(): TReadOnlyProperty<string>;
    /**
     * To support voicing.
     */
    getVoicingDetailsContent(): string;
    /**
     * To support voicing.
     */
    getVoicingHintContent(): TReadOnlyProperty<string>;
}
export default HomeScreenView;
