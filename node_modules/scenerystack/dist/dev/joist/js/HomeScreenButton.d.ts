/**
 * A HomeScreenButton is displayed on the HomeScreen for choosing a screen. The button can be in a selected or
 * unselected state - it's large with a yellow frame in its selected state, and small in its unselected state.
 * Selecting the button when in its "selected" state will result in that screen being chosen.
 *
 * @author Michael Kauzmann (PhET Interactive Simulations)
 * @author Chris Klusendorf (PhET Interactive Simulations)
 */
import '../../globals.js';
import type IntentionalAny from '../../phet-core/js/types/IntentionalAny.js';
import type PickRequired from '../../phet-core/js/types/PickRequired.js';
import { type VoicingOptions } from '../../scenery/js/accessibility/voicing/Voicing.js';
import VBox, { type VBoxOptions } from '../../scenery/js/layout/nodes/VBox.js';
import type HomeScreenModel from './HomeScreenModel.js';
import type Screen from './Screen.js';
type SelfOptions = {
    showUnselectedHomeScreenIconFrame?: boolean;
};
type ParentOptions = VoicingOptions & VBoxOptions;
export type HomeScreenButtonOptions = SelfOptions & ParentOptions & PickRequired<ParentOptions, 'tandem'>;
declare const HomeScreenButton_base: typeof VBox & import("../../phet-core.js").Constructor<import("../../scenery/js/accessibility/voicing/Voicing.js").TVoicing<VBox>>;
declare class HomeScreenButton extends HomeScreenButton_base {
    readonly screen: Screen<IntentionalAny, IntentionalAny>;
    constructor(screen: Screen<IntentionalAny, IntentionalAny>, homeScreenModel: HomeScreenModel, providedOptions?: HomeScreenButtonOptions);
}
export default HomeScreenButton;
