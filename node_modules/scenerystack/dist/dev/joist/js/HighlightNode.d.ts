/**
 * Highlight node for navigation bar screen buttons, phet button, etc.
 *
 * @author Sam Reid (PhET Interactive Simulations)
 * @author Chris Malley (PixelZoom, Inc.)
 */
import '../../globals.js';
import HBox, { type HBoxOptions } from '../../scenery/js/layout/nodes/HBox.js';
import type TColor from '../../scenery/js/util/TColor.js';
type SelfOptions = {
    highlightWidth?: number;
    fill?: TColor;
};
type HighlightNodeOptions = SelfOptions & HBoxOptions;
declare class HighlightNode extends HBox {
    /**
     * @param width - can be mutated with the `spacing` property.
     * @param height
     * @param [providedOptions]
     */
    constructor(width: number, height: number, providedOptions?: HighlightNodeOptions);
}
export default HighlightNode;
