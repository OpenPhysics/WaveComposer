import '../../../globals.js';
import '../../../chipper/js/browser/initialize-globals.js';
import type TProperty from '../../../axon/js/TProperty.js';
import type { TReadOnlyProperty } from '../../../axon/js/TReadOnlyProperty.js';
import { type EmptySelfOptions } from '../../../phet-core/js/optionize.js';
import type StrictOmit from '../../../phet-core/js/types/StrictOmit.js';
import Node, { type NodeOptions } from '../../../scenery/js/nodes/Node.js';
import type LookAndFeel from '../LookAndFeel.js';
import { type AnyScreen } from '../Screen.js';
type VoicingToolbarOptions = EmptySelfOptions & StrictOmit<NodeOptions, 'isDisposable'>;
declare class VoicingToolbar extends Node {
    private readonly isEnabledProperty;
    private readonly backgroundRectangle;
    readonly rightPositionProperty: TProperty<number>;
    private rightDestinationPosition;
    private readonly openProperty;
    private readonly isShowingProperty;
    private layoutScale;
    private readonly menuContent;
    private readonly openButton;
    private readonly contentWidth;
    private readonly contentMargin;
    constructor(enabledProperty: TReadOnlyProperty<boolean>, selectedScreenProperty: TReadOnlyProperty<AnyScreen>, lookAndFeel: LookAndFeel, providedOptions?: VoicingToolbarOptions);
    /**
     * Returns the width of the VoicingToolbar that can be seen on screen. This can be any value from the full width of the
     * VoicingToolbar to zero width, depending on whether it is open, closed, removed entirely, or animating.
     */
    getDisplayedWidth(): number;
    private getHiddenPosition;
    /**
     * Update rightDestinationPosition so that the VoicingToolbar will animate towards opening, closing, or being removed
     * entirely from view.
     */
    private updateDestinationPosition;
    /**
     * Animated the VoicingToolbar as it opens and closes.
     */
    private step;
    /**
     * Layout for the VoicingToolbar, called whenever position changes or self is resized.
     */
    layout(scale: number, height: number): void;
}
export default VoicingToolbar;
