/**
 * Generate a rasterized screenshot for a simulation using scenery's built-in machinery.
 * Used in phet-io as well as PhetMenu (optionally)
 *
 * @author Sam Reid (PhET Interactive Simulations)
 */
import '../../globals.js';
import type Sim from './Sim.js';
declare class ScreenshotGenerator {
    private static generateScreenshotAtIncreasedResolution;
    private static renderAtScale;
    static generateScreenshot(sim: Sim, mimeType?: string): string;
}
export default ScreenshotGenerator;
