export default OpenRange;
declare class OpenRange extends Range {
    /**
     * @param {number} min - the minimum value of the range
     * @param {number} max - the maximum value of the range
     * @param {Object} [options]
     */
    constructor(min: number, max: number, options?: Object);
    openMin: any;
    openMax: any;
    /**
     * OpenRange override for setMinMax. Ensures that min is strictly less than max.
     * @override
     * @public
     * @param  {number} min
     * @param  {number} max
     */
    public override setMinMax(min: number, max: number): void;
    /**
     * Determines if this range is equal to other range.
     * @public
     * @param {Range} other
     * @returns {boolean}
     */
    public equals(other: Range): boolean;
}
import Range from './Range.js';
