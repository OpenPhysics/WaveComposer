/**
 * Represents a segment in the graph (connects to vertices on both ends)
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import Pool, { TPoolable } from '../../../phet-core/js/Pool.js';
import HalfEdge, { SerializedHalfEdge } from './HalfEdge.js';
import Segment, { SerializedSegment } from '../segments/Segment.js';
import Vertex from './Vertex.js';
import IntentionalAny from '../../../phet-core/js/types/IntentionalAny.js';
export type SerializedEdge = {
    type: 'Edge';
    id: number;
    segment: SerializedSegment;
    startVertex: number | null;
    endVertex: number | null;
    signedAreaFragment: number;
    forwardHalf: SerializedHalfEdge;
    reversedHalf: SerializedHalfEdge;
    visited: boolean;
    data: IntentionalAny;
};
export default class Edge implements TPoolable {
    readonly id: number;
    segment: Segment;
    startVertex: Vertex;
    endVertex: Vertex;
    signedAreaFragment: number;
    forwardHalf: HalfEdge;
    reversedHalf: HalfEdge;
    visited: boolean;
    data: IntentionalAny;
    internalData: IntentionalAny;
    /**
     * (kite-internal)
     *
     * NOTE: Use Edge.pool.create for most usage instead of using the constructor directly.
     */
    constructor(segment: Segment, startVertex: Vertex, endVertex: Vertex);
    /**
     * Similar to a usual constructor, but is set up so it can be called multiple times (with dispose() in-between) to
     * support pooling.
     */
    private initialize;
    /**
     * Returns an object form that can be turned back into a segment with the corresponding deserialize method.
     */
    serialize(): SerializedEdge;
    /**
     * Removes references (so it can allow other objects to be GC'ed or pooled), and frees itself to the pool so it
     * can be reused.
     */
    dispose(): void;
    /**
     * Returns the other vertex associated with an edge.
     */
    getOtherVertex(vertex: Vertex): Vertex;
    /**
     * Update possibly reversed vertex references (since they may be updated)
     */
    updateReferences(): void;
    freeToPool(): void;
    static pool: Pool<typeof Edge>;
}
