/**
 * Represents a path up to a PDOMInstance.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
import Trail from '../../util/Trail.js';
import PDOMInstance from './PDOMInstance.js';
export default class PartialPDOMTrail {
    readonly pdomInstance: PDOMInstance;
    readonly trail: Trail;
    readonly isRoot: boolean;
    readonly fullTrail: Trail;
    constructor(pdomInstance: PDOMInstance, trail: Trail, isRoot: boolean);
}
