/**
 * DropShadow filter
 *
 * EXPERIMENTAL! DO not use in production code yet
 *
 * TODO: preventFit OR handle bounds increase (or both) https://github.com/phetsims/scenery/issues/1581
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import Vector2 from '../../../dot/js/Vector2.js';
import Filter from '../filters/Filter.js';
import type TColor from '../util/TColor.js';
export default class DropShadow extends Filter {
    private readonly offset;
    private readonly blurRadius;
    private readonly color;
    private readonly colorCSS;
    /**
     * @param offset
     * @param blurRadius
     * @param color
     * @param [filterRegionPercentage]
     */
    constructor(offset: Vector2, blurRadius: number, color: TColor, filterRegionPercentage?: number);
    /**
     * Returns the CSS-style filter substring specific to this single filter, e.g. `grayscale(1)`. This should be used for
     * both DOM elements (https://developer.mozilla.org/en-US/docs/Web/CSS/filter) and when supported, Canvas
     * (https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/filter).
     */
    getCSSFilterString(): string;
    isDOMCompatible(): boolean;
    applyCanvasFilter(): void;
    applySVGFilter(svgFilter: SVGFilterElement, inName: string, resultName?: string): void;
}
