import '../../../globals.js';
/**
 * Returns the pointer type (for the pointer spec) for a given MS pointer event.
 *
 * (scenery-internal)
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
export declare const getMSPointerType: (event: PointerEvent) => string;
