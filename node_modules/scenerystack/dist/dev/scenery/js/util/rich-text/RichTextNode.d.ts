/**
 * A leaf node element in the RichText, which will display a Node (e.g. with the `nodes` or `tags` feature)
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
import Pool, { TPoolable } from '../../../../phet-core/js/Pool.js';
import Node from '../../nodes/Node.js';
declare const RichTextNode_base: typeof Node & import("../../../../phet-core.js").Constructor<{
    readonly isCleanable: boolean;
    clean(): void;
}>;
export default class RichTextNode extends RichTextNode_base implements TPoolable {
    readonly leftSpacing = 0;
    readonly rightSpacing = 0;
    constructor(content: Node);
    initialize(content: Node): this;
    /**
     * Cleans references that could cause memory leaks (as those things may contain other references).
     */
    clean(): void;
    /**
     * Whether this leaf will fit in the specified amount of space
     */
    fitsIn(widthAvailable: number): boolean;
    freeToPool(): void;
    static readonly pool: Pool<typeof RichTextNode>;
}
export {};
