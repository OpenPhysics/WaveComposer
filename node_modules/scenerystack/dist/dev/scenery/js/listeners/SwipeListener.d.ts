export default SwipeListener;
declare class SwipeListener {
    /**
     * @param {Input} input
     */
    constructor(input: Input);
    _pointer: any;
    downPoint: any;
    downEvent: any;
    enabled: boolean;
    lastPoint: any;
    currentPoint: any;
    velocity: any;
    swipeDistance: any;
    firstUp: boolean;
    timeSinceLastDown: number;
    downPointers: any[];
    holdingTime: number;
    focusedNode: import("../nodes/Node.js").default | null;
    handleEventListener: {
        down: (event: any) => void;
    };
    _attachedPointerListener: {
        up: (event: any) => void;
        move: (event: any) => void;
        interrupt: (event: any) => void;
        cancel: (event: any) => void;
    };
    _pointerListener: {
        up: (event: any) => void;
        move: (event: any) => void;
        interrupt: () => void;
        cancel: () => void;
    };
    /**
     * @public (scenery-internal)
     * @param event
     */
    public handleDown(event: any): void;
    previousPoint: any;
    /**
     * @public
     * @param event
     */
    public up(event: any): void;
    /**
     * Step the listener, updating timers used to determine swipe speeds and
     * double tap gestures.
     * @param dt
     * @private
     */
    private step;
    /**
     * Ends a swipe gesture, removing listeners and clearing references.
     * @private
     */
    private endSwipe;
    /**
     * Detach the Pointer listener that is observing movement after a press-and-hold gesture.
     * This allows you to forward the down event to another listener if you don't want to
     * re-implement an interaction with swipeMove. This does not remove the listener from the Pointer,
     * just detaches it so that another listener can be attached.
     * @public
     */
    public detachPointerListener(): void;
    /**
     * Interrupt this listener.
     * @public
     */
    public interrupt(): void;
}
