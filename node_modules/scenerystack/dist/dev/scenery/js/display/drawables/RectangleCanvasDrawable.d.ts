export default RectangleCanvasDrawable;
declare const RectangleCanvasDrawable_base: {
    new (): {
        [x: string]: any;
        initialize(renderer: number, instance: Instance, ...args: any[]): void;
        fillCallback: any;
        strokeCallback: any;
        fillObserver: any;
        strokeObserver: any;
        dispose(): void;
        markDirtyFill(): void;
        markDirtyStroke(): void;
        markDirtyLineWidth(): void;
        markDirtyLineOptions(): void;
        markDirtyCachedPaints(): void;
    };
    [x: string]: any;
};
declare class RectangleCanvasDrawable extends RectangleCanvasDrawable_base {
    /**
     * Convenience function for drawing a rectangular path (with our Rectangle node's parameters) to the Canvas context.
     * @private
     *
     * @param {CanvasRenderingContext2D} context - To execute drawing commands on.
     * @param {Node} node - The node whose rectangle we want to draw
     */
    private writeRectangularPath;
    /**
     * Paints this drawable to a Canvas (the wrapper contains both a Canvas reference and its drawing context).
     * @public
     *
     * Assumes that the Canvas's context is already in the proper local coordinate frame for the node, and that any
     * other required effects (opacity, clipping, etc.) have already been prepared.
     *
     * This is part of the CanvasSelfDrawable API required to be implemented for subtypes.
     *
     * @param {CanvasContextWrapper} wrapper - Contains the Canvas and its drawing context
     * @param {scenery.Node} node - Our node that is being drawn
     * @param {Matrix3} matrix - The transformation matrix applied for this node's coordinate system.
     */
    public paintCanvas(wrapper: CanvasContextWrapper, node: scenery.Node, matrix: Matrix3): void;
    /**
     * @public
     */
    public markDirtyRectangle(): void;
    /**
     * @public
     */
    public markDirtyX(): void;
    /**
     * @public
     */
    public markDirtyY(): void;
    /**
     * @public
     */
    public markDirtyWidth(): void;
    /**
     * @public
     */
    public markDirtyHeight(): void;
    /**
     * @public
     */
    public markDirtyCornerXRadius(): void;
    /**
     * @public
     */
    public markDirtyCornerYRadius(): void;
}
