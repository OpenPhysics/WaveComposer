export default SelfDrawable;
declare class SelfDrawable extends Drawable {
    /**
     * We have enough concrete types that want a fallback constructor to this, so we'll provide it for convenience.
     *
     * @param {number} renderer
     * @param {Instance} instance
     */
    constructor(renderer: number, instance: Instance);
    /**
     * @public
     *
     * @param {number} renderer
     * @param {Instance} instance
     * @returns {SelfDrawable}
     */
    public initialize(renderer: number, instance: Instance): SelfDrawable;
    drawableVisibilityListener: any;
    instance: any;
    node: any;
    /**
     * @public
     */
    public updateSelfVisibility(): void;
}
import Drawable from '../display/Drawable.js';
