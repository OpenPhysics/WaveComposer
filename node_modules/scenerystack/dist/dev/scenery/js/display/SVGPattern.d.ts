import '../../../globals.js';
import Pool, { TPoolable } from '../../../phet-core/js/Pool.js';
import type Pattern from '../util/Pattern.js';
export default class SVGPattern implements TPoolable {
    definition: SVGPatternElement;
    private imageElement;
    constructor(pattern: Pattern);
    initialize(pattern: Pattern): this;
    /**
     * Called from SVGBlock, matches other paints.
     */
    update(): void;
    /**
     * Disposes, so that it can be reused from the pool.
     */
    dispose(): void;
    freeToPool(): void;
    static readonly pool: Pool<typeof SVGPattern>;
}
