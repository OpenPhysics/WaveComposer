/**
 * Plays a sine wave using an Oscillator Node
 *
 * @author Sam Reid (PhET Interactive Simulations)
 */
import '../../../globals.js';
import ReadOnlyProperty from '../../../axon/js/ReadOnlyProperty.js';
import SoundGenerator, { SoundGeneratorOptions } from './SoundGenerator.js';
type SelfOptions = {
    initialOutputLevel?: number;
    oscillatorType?: OscillatorType;
};
type WaveGeneratorOptions = SelfOptions & SoundGeneratorOptions;
declare class WaveGenerator extends SoundGenerator {
    private oscillator;
    constructor(frequencyProperty: ReadOnlyProperty<number>, amplitudeProperty: ReadOnlyProperty<number>, providedOptions?: WaveGeneratorOptions);
}
export default WaveGenerator;
