/**
 * `phetAudioContext` is a singleton instance of a Web Audio context that is used for all Web Audio interaction within
 * a sim.
 *
 * @author John Blanco
 */
import '../../globals.js';
declare let phetAudioContext: AudioContext;
export default phetAudioContext;
