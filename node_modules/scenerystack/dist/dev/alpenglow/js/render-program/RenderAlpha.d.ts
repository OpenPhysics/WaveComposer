/**
 * RenderProgram for alpha (an opacity) applied to a RenderProgram
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import Vector4 from '../../../dot/js/Vector4.js';
import { RenderProgram, SerializedRenderProgram } from './RenderProgram.js';
import type { RenderEvaluationContext } from './RenderEvaluationContext.js';
import { RenderInstruction } from './RenderInstruction.js';
export declare class RenderAlpha extends RenderProgram {
    readonly program: RenderProgram;
    readonly alpha: number;
    constructor(program: RenderProgram, alpha: number);
    getName(): string;
    withChildren(children: RenderProgram[]): RenderAlpha;
    protected equalsTyped(other: this): boolean;
    protected getSimplified(children: RenderProgram[]): RenderProgram | null;
    evaluate(context: RenderEvaluationContext): Vector4;
    writeInstructions(instructions: RenderInstruction[]): void;
    protected getExtraDebugString(): string;
    serialize(): SerializedRenderAlpha;
}
export type SerializedRenderAlpha = {
    type: 'RenderAlpha';
    program: SerializedRenderProgram;
    alpha: number;
};
