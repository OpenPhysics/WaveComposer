/**
 * A gradient stop for linear/radial gradients
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import Vector4 from '../../../dot/js/Vector4.js';
import { RenderProgram, SerializedRenderProgram } from './RenderProgram.js';
import type { RenderEvaluationContext } from './RenderEvaluationContext.js';
export declare class RenderGradientStop {
    readonly ratio: number;
    readonly program: RenderProgram;
    constructor(ratio: number, program: RenderProgram);
    static evaluate(context: RenderEvaluationContext, stops: RenderGradientStop[], t: number): Vector4;
    withProgram(program: RenderProgram): RenderGradientStop;
    serialize(): SerializedRenderGradientStop;
}
export type SerializedRenderGradientStop = {
    ratio: number;
    program: SerializedRenderProgram;
};
