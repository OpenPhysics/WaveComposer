/**
 * RenderProgram to convert between color spaces. Should not change whether something is transparent or opaque
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import Vector4 from '../../../dot/js/Vector4.js';
import Constructor from '../../../phet-core/js/types/Constructor.js';
import { RenderProgram, SerializedRenderProgram } from './RenderProgram.js';
import { RenderEvaluationContext } from './RenderEvaluationContext.js';
export declare abstract class RenderColorSpaceConversion extends RenderProgram {
    readonly program: RenderProgram;
    readonly convert: (color: Vector4) => Vector4;
    inverse?: Constructor<RenderColorSpaceConversion>;
    protected constructor(program: RenderProgram, convert: (color: Vector4) => Vector4);
    protected getSimplified(children: RenderProgram[]): RenderProgram | null;
    evaluate(context: RenderEvaluationContext): Vector4;
    serialize(): SerializedRenderColorSpaceConversion;
}
export type SerializedRenderColorSpaceConversion = {
    type: 'RenderColorSpaceConversion';
    subtype: string;
    program: SerializedRenderProgram;
};
