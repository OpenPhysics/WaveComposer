/**
 * Logic for a radial gradient
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import Vector2 from '../../../dot/js/Vector2.js';
import Matrix3 from '../../../dot/js/Matrix3.js';
import { RadialGradientType } from './RadialGradientType.js';
import { RenderExtend } from './RenderExtend.js';
import type { RenderEvaluationContext } from './RenderEvaluationContext.js';
import { RenderRadialGradientAccuracy } from './RenderRadialGradientAccuracy.js';
export declare class RenderRadialGradientLogic {
    readonly conicTransform: Matrix3;
    readonly focalX: number;
    readonly radius: number;
    readonly kind: RadialGradientType;
    readonly isSwapped: boolean;
    readonly ratios: number[];
    readonly extend: RenderExtend;
    readonly accuracy: RenderRadialGradientAccuracy;
    constructor(conicTransform: Matrix3, focalX: number, radius: number, kind: RadialGradientType, isSwapped: boolean, ratios: number[], // should be sorted!!
    extend: RenderExtend, accuracy: RenderRadialGradientAccuracy);
    static from(transform: Matrix3, start: Vector2, startRadius: number, end: Vector2, endRadius: number, ratios: number[], // should be sorted!!
    extend: RenderExtend, accuracy: RenderRadialGradientAccuracy): RenderRadialGradientLogic;
    equals(other: RenderRadialGradientLogic): boolean;
    computeLinearValue(context: RenderEvaluationContext): number;
    isLinear(): boolean;
}
