/**
 * A optimized form for constructing RenderPrograms with their RenderPathBooleans simplified.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import type { RenderPath } from './RenderPath.js';
import type { RenderProgram } from './RenderProgram.js';
import { RenderStack } from './RenderStack.js';
import { RenderDepthSort } from './RenderDepthSort.js';
import { RenderPathBoolean } from './RenderPathBoolean.js';
export declare class RenderPathReplacer {
    readonly program: RenderProgram;
    private readonly trailsMap;
    private readonly permanentChildrenMap;
    constructor(program: RenderProgram);
    replace(includedPaths: Set<RenderPath>): RenderProgram;
    private replaceRecurse;
    private initialRecurse;
    private addTrail;
    private addPermanentChild;
    static isContainer(program: RenderProgram): program is (RenderStack | RenderDepthSort);
    static isTransparentOutside(program: RenderProgram): program is RenderPathBoolean;
}
