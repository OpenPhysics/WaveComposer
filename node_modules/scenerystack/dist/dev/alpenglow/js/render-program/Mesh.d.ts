/**
 * Represents mesh data that can be handled in different ways.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import Vector3 from '../../../dot/js/Vector3.js';
export declare class Mesh {
    readonly name: string | null;
    readonly vertices: Vector3[];
    readonly uvs: Vector3[];
    readonly normals: Vector3[];
    readonly faces: MeshFace[];
    readonly lines: MeshLine[];
    constructor(name?: string | null);
    static loadOBJ(str: string): Mesh[];
}
export declare class MeshFace {
    readonly material: string;
    readonly group: string;
    readonly smoothingGroup: number;
    readonly vertexIndices: number[];
    readonly uvIndices: number[];
    readonly normalIndices: number[];
    constructor(material: string, group: string, smoothingGroup: number);
}
export declare class MeshLine {
    readonly vertexIndices: number[];
    readonly uvIndices: number[];
}
