/**
 * RenderProgram for a classic radial gradient.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import Matrix3 from '../../../dot/js/Matrix3.js';
import Vector2 from '../../../dot/js/Vector2.js';
import Vector4 from '../../../dot/js/Vector4.js';
import { RenderGradientStop, SerializedRenderGradientStop } from './RenderGradientStop.js';
import { RenderProgram } from './RenderProgram.js';
import { RenderExtend } from './RenderExtend.js';
import type { RenderEvaluationContext } from './RenderEvaluationContext.js';
import { RenderInstruction } from './RenderInstruction.js';
import { RenderableFace } from '../raster/RenderableFace.js';
import { RenderRadialGradientLogic } from './RenderRadialGradientLogic.js';
import { RenderRadialGradientAccuracy } from './RenderRadialGradientAccuracy.js';
export declare class RenderRadialGradient extends RenderProgram {
    readonly transform: Matrix3;
    readonly start: Vector2;
    readonly startRadius: number;
    readonly end: Vector2;
    readonly endRadius: number;
    readonly stops: RenderGradientStop[];
    readonly extend: RenderExtend;
    readonly accuracy: RenderRadialGradientAccuracy;
    private logic;
    constructor(transform: Matrix3, start: Vector2, startRadius: number, end: Vector2, endRadius: number, stops: RenderGradientStop[], // should be sorted!!
    extend: RenderExtend, accuracy: RenderRadialGradientAccuracy, logic?: RenderRadialGradientLogic);
    getName(): string;
    withChildren(children: RenderProgram[]): RenderRadialGradient;
    isSplittable(): boolean;
    transformed(transform: Matrix3): RenderProgram;
    protected equalsTyped(other: this): boolean;
    protected getSimplified(children: RenderProgram[]): RenderProgram | null;
    getLogic(): RenderRadialGradientLogic;
    evaluate(context: RenderEvaluationContext): Vector4;
    writeInstructions(instructions: RenderInstruction[]): void;
    split(face: RenderableFace): RenderableFace[];
    serialize(): SerializedRenderRadialGradient;
}
export type SerializedRenderRadialGradient = {
    type: 'RenderRadialGradient';
    transform: number[];
    start: number[];
    startRadius: number;
    end: number[];
    endRadius: number;
    stops: SerializedRenderGradientStop[];
    extend: RenderExtend;
    accuracy: RenderRadialGradientAccuracy;
};
