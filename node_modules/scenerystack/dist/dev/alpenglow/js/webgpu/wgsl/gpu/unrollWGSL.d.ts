/**
 * A template that unrolls a simple loop over a range of numbers.
 *
 * Supports both forward and backward iteration (start < end, or start > end).
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../../globals.js';
import { WGSLStatements } from '../WGSLString.js';
export declare const unrollWGSL: (start: number, end: number, callback: (i: number, isFirst: boolean, isLast: boolean) => WGSLStatements) => WGSLStatements;
