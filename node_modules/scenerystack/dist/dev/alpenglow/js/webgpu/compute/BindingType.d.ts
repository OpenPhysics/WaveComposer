/**
 * TODO: doc
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
export declare abstract class BindingType {
    protected abstract mutateBindGroupLayoutEntry(entry: GPUBindGroupLayoutEntry): void;
    abstract combined(other: BindingType): BindingType | null;
    abstract toDebugString(): string;
    getBindGroupLayoutEntry(index: number): GPUBindGroupLayoutEntry;
}
