/**
 * TODO: doc
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
import type { Binding } from './Binding.js';
export declare abstract class Resource {
    readonly resource: GPUBuffer | GPUTextureView;
    readonly id: number;
    constructor(resource: GPUBuffer | GPUTextureView);
    abstract getBindGroupEntry(binding: Binding): GPUBindGroupEntry;
    abstract toDebugString(): string;
}
