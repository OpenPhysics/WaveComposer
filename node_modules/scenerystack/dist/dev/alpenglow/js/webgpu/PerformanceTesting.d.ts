/**
 * For testing overlapping (parallel) execution of shader stages that don't write to the same memory.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
export declare class PerformanceTesting {
    static loopRadixSortTest(combineStrategy: boolean, separateComputePasses: boolean): Promise<void>;
}
