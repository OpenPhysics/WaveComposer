/**
 * Applies a histogram operation to an array.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../../globals.js';
import type { BufferArraySlot } from '../../compute/BufferArraySlot.js';
import { mainHistogramWGSLOptions } from '../../wgsl/gpu/mainHistogramWGSL.js';
import type { PipelineBlueprintOptions } from '../../compute/PipelineBlueprint.js';
import { DirectModule } from '../../compute/DirectModule.js';
export type HistogramModuleOptions<T> = {
    input: BufferArraySlot<T>;
    output: BufferArraySlot<number>;
} & mainHistogramWGSLOptions<T> & PipelineBlueprintOptions;
export declare const HISTOGRAM_MODULE_DEFAULTS: {
    readonly lengthExpression: null;
    readonly log: false;
};
export declare class HistogramModule<T> extends DirectModule<number> {
    readonly input: BufferArraySlot<T>;
    readonly output: BufferArraySlot<number>;
    constructor(providedOptions: HistogramModuleOptions<T>);
}
