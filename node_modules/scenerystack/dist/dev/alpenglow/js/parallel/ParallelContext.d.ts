/**
 * Contains all the data/methods needed by a kernel to run in parallel. Passed to the kernel execution function, so that
 * it can make equivalent calls similar to the WGPU calls.
 *
 * See ParallelExecutor for more high-level documentation.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import Vector3 from '../../../dot/js/Vector3.js';
import type { ParallelWorkgroupArray } from './ParallelWorkgroupArray.js';
import type { ParallelKernel } from './ParallelKernel.js';
import type { ParallelWorkgroup } from './ParallelWorkgroup.js';
import type { ParallelExecutor } from './ParallelExecutor.js';
export type BaseWorkgroupValues = Record<string, ParallelWorkgroupArray<unknown> | number>;
export declare class ParallelContext<WorkgroupValues extends BaseWorkgroupValues> {
    readonly kernel: ParallelKernel<WorkgroupValues>;
    readonly localId: Vector3;
    readonly workgroup: ParallelWorkgroup<WorkgroupValues>;
    readonly executor: ParallelExecutor<WorkgroupValues>;
    readonly globalId: Vector3;
    readonly localIndex: number;
    readonly workgroupId: Vector3;
    readonly workgroupValues: WorkgroupValues;
    constructor(kernel: ParallelKernel<WorkgroupValues>, localId: Vector3, workgroup: ParallelWorkgroup<WorkgroupValues>, executor: ParallelExecutor<WorkgroupValues>);
    start(): Promise<void>;
    workgroupBarrier(): Promise<void>;
    storageBarrier(): Promise<void>;
    afterSet(): Promise<void>;
    afterGet(): Promise<void>;
}
