/**
 * Represents a WGPU-like kernel that can be executed in parallel. Designed so that we can test WGPU-like code, but use
 * the debugging facilities of the browser to debug it.
 *
 * See ParallelExecutor for more high-level documentation.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import { BaseWorkgroupValues, ParallelContext } from './ParallelContext.js';
import type { ParallelStorageArray } from './ParallelStorageArray.js';
export declare class ParallelKernel<WorkgroupValues extends BaseWorkgroupValues = Record<string, never>> {
    readonly execute: (context: ParallelContext<WorkgroupValues>) => Promise<void>;
    readonly createWorkgroupValues: () => WorkgroupValues;
    readonly storageArrays: ParallelStorageArray<unknown>[];
    readonly workgroupX: number;
    readonly workgroupY: number;
    readonly workgroupZ: number;
    constructor(execute: (context: ParallelContext<WorkgroupValues>) => Promise<void>, createWorkgroupValues: () => WorkgroupValues, storageArrays: ParallelStorageArray<unknown>[], workgroupX?: number, workgroupY?: number, workgroupZ?: number);
}
