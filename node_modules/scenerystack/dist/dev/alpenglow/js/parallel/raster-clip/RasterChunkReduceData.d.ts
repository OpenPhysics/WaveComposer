/**
 * (Partial) data for a single clipped chunk, which is reduced and then when complete (isFirstEdge && isLastEdge) is
 * applied to the clipped chunk.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
import type { ByteEncoder } from '../../webgpu/compute/ByteEncoder.js';
import { RasterClippedChunk } from './RasterClippedChunk.js';
export declare class RasterChunkReduceData {
    readonly clippedChunkIndex: number;
    readonly area: number;
    readonly isFirstEdge: boolean;
    readonly isLastEdge: boolean;
    readonly minX: number;
    readonly minY: number;
    readonly maxX: number;
    readonly maxY: number;
    readonly minXCount: number;
    readonly minYCount: number;
    readonly maxXCount: number;
    readonly maxYCount: number;
    constructor(clippedChunkIndex: number, area: number, isFirstEdge: boolean, isLastEdge: boolean, minX: number, minY: number, maxX: number, maxY: number, minXCount: number, minYCount: number, maxXCount: number, maxYCount: number);
    isValid(): boolean;
    toString(): string;
    static readonly ENCODING_BYTE_LENGTH: number;
    writeEncoding(encoder: ByteEncoder): void;
    static readEncoding(arrayBuffer: ArrayBuffer, byteOffset: number): RasterChunkReduceData;
    static fromArrayBuffer(arrayBuffer: ArrayBuffer): RasterChunkReduceData[];
    equals(other: RasterChunkReduceData): boolean;
    apply(clippedChunk: RasterClippedChunk): RasterClippedChunk;
    static combine(a: RasterChunkReduceData, b: RasterChunkReduceData): RasterChunkReduceData;
    static readonly INDETERMINATE: RasterChunkReduceData;
    static readonly OUT_OF_RANGE: RasterChunkReduceData;
}
