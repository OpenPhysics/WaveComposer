/**
 * Like RasterChunkReduceQuad, but for the initial case where our left/right values are the same, and we can do a
 * slightly different optimized reduce.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
import { RasterChunkReduceData } from './RasterChunkReduceData.js';
import type { ByteEncoder } from '../../webgpu/compute/ByteEncoder.js';
export declare class RasterChunkReducePair {
    min: RasterChunkReduceData;
    max: RasterChunkReduceData;
    constructor(min: RasterChunkReduceData, max: RasterChunkReduceData);
    isFirstEdge(): boolean;
    isLastEdge(): boolean;
    static combine(a: RasterChunkReducePair, b: RasterChunkReducePair): RasterChunkReducePair;
    toStrings(): string[];
    static readonly ENCODING_BYTE_LENGTH: number;
    writeEncoding(encoder: ByteEncoder): void;
    static readEncoding(arrayBuffer: ArrayBuffer, byteOffset: number): RasterChunkReducePair;
    static fromArrayBuffer(arrayBuffer: ArrayBuffer): RasterChunkReducePair[];
    static readonly INDETERMINATE: RasterChunkReducePair;
    static readonly OUT_OF_RANGE: RasterChunkReducePair;
}
