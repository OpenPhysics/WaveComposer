/**
 * Rasterization prototype for WebGPU, but using the parallel (debuggable) API.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
import { RasterChunk } from './RasterChunk.js';
import { RasterEdge } from './RasterEdge.js';
import { ParallelStorageArray } from '../ParallelStorageArray.js';
import { RasterCompleteChunk } from './RasterCompleteChunk.js';
import { RasterCompleteEdge } from './RasterCompleteEdge.js';
export declare class ParallelRaster {
    static getTestRawInputChunks(): RasterChunk[];
    static getTestRawInputEdges(): RasterEdge[];
    static test(): Promise<void>;
    static process(workgroupSize: number, numInputChunks: number, numInputEdges: number, inputChunks: ParallelStorageArray<RasterChunk>, inputEdges: ParallelStorageArray<RasterEdge>): Promise<{
        reducibleChunks: ParallelStorageArray<RasterChunk>;
        reducibleEdges: ParallelStorageArray<RasterEdge>;
        numReducibleChunks: number;
        numReducibleEdges: number;
        completeChunks: ParallelStorageArray<RasterCompleteChunk>;
        completeEdges: ParallelStorageArray<RasterCompleteEdge>;
        numCompleteChunks: number;
        numCompleteEdges: number;
    }>;
}
