/**
 * Contains path data within a bounds, for a particular RenderProgram
 *
 * Used for the raster-clip input, and the output for reducible chunks (that will be fed back in)
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../../globals.js';
import type { ByteEncoder } from '../../webgpu/compute/ByteEncoder.js';
import type { ParallelStorageArray } from '../ParallelStorageArray.js';
import type { RasterEdge } from './RasterEdge.js';
export declare class RasterChunk {
    readonly renderProgramIndex: number;
    readonly needsFace: boolean;
    readonly isConstant: boolean;
    readonly edgesOffset: number;
    readonly numEdges: number;
    readonly minX: number;
    readonly minY: number;
    readonly maxX: number;
    readonly maxY: number;
    readonly minXCount: number;
    readonly minYCount: number;
    readonly maxXCount: number;
    readonly maxYCount: number;
    constructor(renderProgramIndex: number, needsFace: boolean, isConstant: boolean, edgesOffset: number, numEdges: number, minX: number, minY: number, maxX: number, maxY: number, minXCount: number, minYCount: number, maxXCount: number, maxYCount: number);
    withEdgeInfo(startIndex: number, endIndex: number): RasterChunk;
    toString(): string;
    static readonly ENCODING_BYTE_LENGTH: number;
    writeEncoding(encoder: ByteEncoder): void;
    static readEncoding(arrayBuffer: ArrayBuffer, byteOffset: number): RasterChunk;
    static fromArrayBuffer(arrayBuffer: ArrayBuffer): RasterChunk[];
    static readonly INDETERMINATE: RasterChunk;
    static validate(chunks: ParallelStorageArray<RasterChunk>, edges: ParallelStorageArray<RasterEdge>, numChunks: number, numEdges: number): void;
}
