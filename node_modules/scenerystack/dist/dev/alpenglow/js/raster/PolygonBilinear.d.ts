/**
 * Utilities for the bilinear/tent filter for polygons
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import Vector2 from '../../../dot/js/Vector2.js';
import type { LinearEdge } from '../cag/LinearEdge.js';
export declare class PolygonBilinear {
    static evaluate(p0x: number, p0y: number, p1x: number, p1y: number): number;
    private static getSign;
    static evaluatePolygons(polygons: Vector2[][], pointX: number, pointY: number, minX: number, minY: number): number;
    static evaluateLinearEdges(edges: LinearEdge[], pointX: number, pointY: number, minX: number, minY: number): number;
}
