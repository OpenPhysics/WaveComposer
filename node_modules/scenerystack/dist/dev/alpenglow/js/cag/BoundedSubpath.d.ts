/**
 * A single loop of a RenderPath, with computed bounds.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import Bounds2 from '../../../dot/js/Bounds2.js';
import type Vector2 from '../../../dot/js/Vector2.js';
import type { RenderPath } from '../render-program/RenderPath.js';
export declare class BoundedSubpath {
    readonly path: RenderPath;
    readonly subpath: Vector2[];
    readonly bounds: Bounds2;
    constructor(path: RenderPath, subpath: Vector2[]);
    static fromPathSet(paths: Set<RenderPath>): BoundedSubpath[];
}
