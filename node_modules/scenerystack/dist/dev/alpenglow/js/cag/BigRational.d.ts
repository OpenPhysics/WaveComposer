/**
 * Contains an import-style snippet of shader code, with dependencies on other snippets.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
export declare class BigRational {
    numerator: bigint;
    denominator: bigint;
    constructor(numerator: bigint | number, denominator: bigint | number);
    copy(): BigRational;
    plus(rational: BigRational): BigRational;
    minus(rational: BigRational): BigRational;
    times(rational: BigRational): BigRational;
    dividedBy(rational: BigRational): BigRational;
    reduce(): void;
    reduced(): BigRational;
    isZero(): boolean;
    isNegative(): boolean;
    isPositive(): boolean;
    ratioTest(): number;
    equalsCrossMul(other: BigRational): boolean;
    compareCrossMul(other: BigRational): number;
    equals(other: BigRational): boolean;
    toFloat(precision?: number): number;
    toString(): string;
    static readonly ZERO: BigRational;
    static readonly ONE: BigRational;
    static whole(numerator: number | bigint): BigRational;
    static inverse(numerator: number | bigint): BigRational;
    static gcdBigInt(a: bigint, b: bigint): bigint;
}
