/**
 * Representation of a winding map for a face (or an edge as a delta)
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../../globals.js';
import type { RenderPath } from '../render-program/RenderPath.js';
export declare class WindingMap {
    readonly map: Map<RenderPath, number>;
    constructor(map?: Map<RenderPath, number>);
    getWindingNumber(renderPath: RenderPath): number;
    addWindingNumber(renderPath: RenderPath, amount: number): void;
    addWindingMap(windingMap: WindingMap): void;
    equals(windingMap: WindingMap): boolean;
}
