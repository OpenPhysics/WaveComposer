/**
 * Displays a Property of type {number} in a background rectangle.
 *
 * @author Chris Malley (PixelZoom, Inc.)
 */
import '../../globals.js';
import '../../chipper/js/browser/initialize-globals.js';
import { TReadOnlyProperty } from '../../axon/js/TReadOnlyProperty.js';
import Range from '../../dot/js/Range.js';
import StrictOmit from '../../phet-core/js/types/StrictOmit.js';
import Node, { NodeOptions } from '../../scenery/js/nodes/Node.js';
import { RichTextOptions } from '../../scenery/js/nodes/RichText.js';
import { TextOptions } from '../../scenery/js/nodes/Text.js';
import Font from '../../scenery/js/util/Font.js';
import TPaint from '../../scenery/js/util/TPaint.js';
import IOType from '../../tandem/js/types/IOType.js';
import { DualStringNumber, DualValuePattern, NumberFormatOptions } from '../../axon/js/AccessibleStrings.js';
declare const ALIGN_VALUES: readonly ["center", "left", "right"];
type NumberDisplayAlign = typeof ALIGN_VALUES[number];
type NumberFormatter = (n: number) => string | DualStringNumber;
type ValuePattern = string | TReadOnlyProperty<string> | DualValuePattern;
type SelfOptions = {
    align?: NumberDisplayAlign;
    valuePattern?: ValuePattern;
    decimalPlaces?: number | null;
    numberFormatOptions?: NumberFormatOptions;
    numberFormatter?: NumberFormatter | null;
    numberFormatterDependencies?: TReadOnlyProperty<unknown>[];
    useRichText?: boolean;
    useFullHeight?: boolean;
    textOptions?: TextOptions | RichTextOptions;
    xMargin?: number;
    yMargin?: number;
    cornerRadius?: number;
    backgroundFill?: TPaint;
    backgroundStroke?: TPaint;
    backgroundLineWidth?: number;
    backgroundLineDash?: number[];
    minBackgroundWidth?: number;
    noValueString?: string;
    noValueAlign?: NumberDisplayAlign | null;
    noValuePattern?: ValuePattern | null;
};
export type NumberDisplayOptions = SelfOptions & StrictOmit<NodeOptions, 'children'>;
export default class NumberDisplay extends Node {
    private readonly valueText;
    private readonly backgroundNode;
    readonly valueStringProperty: TReadOnlyProperty<string>;
    readonly accessibleValueStringProperty: TReadOnlyProperty<string>;
    private readonly disposeNumberDisplay;
    /**
     * @param numberProperty
     * @param displayRange - this range, with options.decimals or numberFormatter applied, is used to determine
     *                     - the display width. It is unrelated to the range of numberProperty.
     * @param providedOptions
     */
    constructor(numberProperty: TReadOnlyProperty<number | null>, displayRange: Range, providedOptions?: NumberDisplayOptions);
    dispose(): void;
    /**
     * Sets the number text font.
     */
    setNumberFont(font: Font): void;
    set numberFont(value: Font);
    /**
     * Sets the number text fill.
     */
    setNumberFill(fill: TPaint): void;
    set numberFill(value: TPaint);
    /**
     * Sets the background fill.
     */
    setBackgroundFill(fill: TPaint): void;
    set backgroundFill(value: TPaint);
    get backgroundFill(): TPaint;
    /**
     * Gets the background fill.
     */
    getBackgroundFill(): TPaint;
    /**
     * Sets the background stroke.
     */
    setBackgroundStroke(stroke: TPaint): void;
    set backgroundStroke(value: TPaint);
    /**
     * Get the width of the background.
     */
    getBackgroundWidth(): number;
    /**
     * Set the width of the background node.
     */
    setBackgroundWidth(width: number): void;
    get backgroundWidth(): number;
    set backgroundWidth(width: number);
    static readonly NumberDisplayIO: IOType<any, any, any>;
}
export {};
