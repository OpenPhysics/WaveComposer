/**
 * A GaugeNode with a NumberDisplay located in the center bottom half of the GaugeNode to
 * display the numerical value. The NumberDisplay can be hidden but is visible by default.
 *
 * @author Jesse Greenberg
 */
import '../../globals.js';
import { TReadOnlyProperty } from '../../axon/js/TReadOnlyProperty.js';
import Range from '../../dot/js/Range.js';
import GaugeNode, { GaugeNodeOptions } from './GaugeNode.js';
import { NumberDisplayOptions } from './NumberDisplay.js';
type SelfOptions = {
    numberDisplayOptions?: NumberDisplayOptions;
};
export type ValueGaugeNodeOptions = SelfOptions & GaugeNodeOptions;
export default class ValueGaugeNode extends GaugeNode {
    private _numberDisplayVisible;
    private readonly numberDisplay;
    constructor(valueProperty: TReadOnlyProperty<number>, label: TReadOnlyProperty<string>, range: Range, providedOptions?: ValueGaugeNodeOptions);
    /**
     * Sets the visibility of the gauge's NumberDisplay.
     */
    setNumberDisplayVisible(visible: boolean): void;
    set numberDisplayVisible(visible: boolean);
    get numberDisplayVisible(): boolean;
    /**
     * Gets the visibility of the gauge's NumberDisplay.
     */
    getNumberDisplayVisible(): boolean;
    dispose(): void;
}
export {};
