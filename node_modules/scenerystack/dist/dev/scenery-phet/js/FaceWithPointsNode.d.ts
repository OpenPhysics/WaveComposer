/**
 * A face that either smiles or frowns.  When the face is smiling, it displays points awarded next to it.
 *
 * @author John Blanco
 * @author Sam Reid (PhET Interactive Simulations)
 * @author Chris Malley (PixelZoom, Inc.)
 */
import '../../globals.js';
import StrictOmit from '../../phet-core/js/types/StrictOmit.js';
import Node, { NodeOptions } from '../../scenery/js/nodes/Node.js';
import Font from '../../scenery/js/util/Font.js';
import TColor from '../../scenery/js/util/TColor.js';
type PointsAlignment = 'centerBottom' | 'rightBottom' | 'rightCenter';
type SelfOptions = {
    spacing?: number;
    faceDiameter?: number;
    faceOpacity?: number;
    pointsAlignment?: PointsAlignment;
    pointsFont?: Font;
    pointsFill?: TColor;
    pointsStroke?: TColor;
    pointsOpacity?: number;
    showZeroPoints?: boolean;
    points?: number;
};
export type FaceWithPointsNodeOptions = SelfOptions & StrictOmit<NodeOptions, 'children'>;
export default class FaceWithPointsNode extends Node {
    private readonly spacing;
    private readonly pointsAlignment;
    private readonly showZeroPoints;
    private readonly faceNode;
    private readonly pointsNode;
    constructor(providedOptions?: FaceWithPointsNodeOptions);
    smile(): void;
    frown(): void;
    /**
     * Sets the number of points displayed.
     * @param points
     */
    setPoints(points: number): void;
    /**
     * Adjusts position of the points to match the specified value of options.pointsAlignment.
     */
    private updatePointsPosition;
}
export {};
