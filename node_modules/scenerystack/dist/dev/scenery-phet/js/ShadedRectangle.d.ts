/**
 * A rectangle with pseudo-3D shading.
 *
 * @author Jonathan Olson <jonathan.olson@colorado.edu>
 */
import '../../globals.js';
import Bounds2 from '../../dot/js/Bounds2.js';
import Node, { NodeOptions } from '../../scenery/js/nodes/Node.js';
import TColor from '../../scenery/js/util/TColor.js';
type LightSource = 'leftTop' | 'rightTop' | 'leftBottom' | 'rightBottom';
type SelfOptions = {
    baseColor?: TColor;
    lightFactor?: number;
    lighterFactor?: number;
    darkFactor?: number;
    darkerFactor?: number;
    cornerRadius?: number;
    lightSource?: LightSource;
    lightOffset?: number;
    darkOffset?: number;
};
export type ShadedRectangleOptions = SelfOptions & NodeOptions;
export default class ShadedRectangle extends Node {
    private readonly lighterPaintProperty;
    private readonly lightPaintProperty;
    private readonly darkPaintProperty;
    private readonly darkerPaintProperty;
    /**
     * @param rectBounds - takes up rectBounds in size
     * @param [providedOptions]
     */
    constructor(rectBounds: Bounds2, providedOptions?: ShadedRectangleOptions);
    dispose(): void;
}
export {};
