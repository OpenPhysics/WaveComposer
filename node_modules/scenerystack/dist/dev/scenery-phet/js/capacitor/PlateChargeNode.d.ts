export default PlateChargeNode;
declare class PlateChargeNode extends CanvasNode {
    /**
     * @param {Capacitor} capacitor
     * @param {YawPitchModelViewTransform3} modelViewTransform
     * @param {Object} [options]
     */
    constructor(capacitor: Capacitor, modelViewTransform: YawPitchModelViewTransform3, options?: Object);
    capacitor: Capacitor;
    orientation: any;
    modelViewTransform: YawPitchModelViewTransform3;
    polarity: any;
    maxPlateCharge: any;
    opacity: any;
    parentNode: Node;
    /**
     * @param {number} numberOfObjects
     * @param {number} width
     * @param {number} height
     * @private
     *
     * @returns {Dimension2}
     */
    private getGridSize;
    /**
     * Get plate charge from capacitor in the model
     * @public
     *
     * @returns {number} charge
     */
    public getPlateCharge(): number;
    /**
     * Gets the x offset (relative to the plate origin) of the portion of the plate that is facing the vacuum gap
     * @public
     *
     * @returns {number} offset
     */
    public getContactXOrigin(): number;
    /**
     * Gets the width of the portion of the plate that is in contact with air.
     * @public
     *
     * @returns {number}
     */
    public getContactWidth(): number;
    /**
     * Returns true if plate is positively charged
     *
     * @returns {Boolean}
     * @public
     */
    public isPositivelyCharged(): boolean;
    /**
     * Computes number of charges, linearly proportional to plate charge.  If plate charge is less than half of an
     * electron charge, number of charges is zero.
     * @public
     *
     * @param {number} plateCharge
     * @param {number} maxPlateCharge
     * @returns {number}
     */
    public getNumberOfCharges(plateCharge: number, maxPlateCharge: number): number;
}
import CanvasNode from '../../../scenery/js/nodes/CanvasNode.js';
import Node from '../../../scenery/js/nodes/Node.js';
