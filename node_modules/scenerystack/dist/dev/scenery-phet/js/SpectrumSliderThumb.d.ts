/**
 * SpectrumSliderThumb has (a) a thin cursor that lies on the track and (b) a teardrop-shaped handle that drops
 * down below the track and depicts the selected color. Origin is at top center.
 *
 * @author Sam Reid (PhET Interactive Simulations)
 */
import '../../globals.js';
import { TReadOnlyProperty } from '../../axon/js/TReadOnlyProperty.js';
import PickOptional from '../../phet-core/js/types/PickOptional.js';
import Path, { PathOptions } from '../../scenery/js/nodes/Path.js';
import { RectangleOptions } from '../../scenery/js/nodes/Rectangle.js';
import { SpectrumNodeOptions } from './SpectrumNode.js';
type SelfOptions = {
    width?: number;
    height?: number;
    cursorHeight?: number;
    cursorWidth?: number;
    windowCursorOptions?: RectangleOptions;
} & PickOptional<SpectrumNodeOptions, 'valueToColor'>;
export type SpectrumSliderThumbOptions = SelfOptions & PathOptions;
export default class SpectrumSliderThumb extends Path {
    private readonly windowCursor;
    private readonly disposeSpectrumSliderThumb;
    constructor(property: TReadOnlyProperty<number>, providedOptions?: SpectrumSliderThumbOptions);
    /**
     * Position the thumb in the track.
     */
    setCenterY(centerY: number): this;
    dispose(): void;
}
export {};
