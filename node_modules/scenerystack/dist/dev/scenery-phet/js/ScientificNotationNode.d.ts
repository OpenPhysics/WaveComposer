/**
 * ScientificNotationNode displays a number in scientific notation, M x 10^E, where:
 * - M is the mantissa
 * - E is the exponent, a positive or negative integer
 *
 * For example, with 2 decimal places in the mantissa, 0.0002342 would be written as 2.34 x 10^-4.
 *
 * @author Chris Malley (PixelZoom, Inc.)
 */
import '../../globals.js';
import '../../chipper/js/browser/initialize-globals.js';
import { TReadOnlyProperty } from '../../axon/js/TReadOnlyProperty.js';
import StrictOmit from '../../phet-core/js/types/StrictOmit.js';
import Node, { NodeOptions } from '../../scenery/js/nodes/Node.js';
import Font from '../../scenery/js/util/Font.js';
import TColor from '../../scenery/js/util/TColor.js';
type SelfOptions = {
    fill?: TColor;
    font?: Font;
    exponent?: number | null;
    mantissaDecimalPlaces?: number;
    exponentScale?: number;
    showIntegersAsMantissaOnly?: boolean;
    showZeroAsInteger?: boolean;
    showZeroExponent?: boolean;
    exponentXSpacing?: number;
    exponentYOffset?: number;
    capHeightScale?: number;
    nullValueString?: string;
};
export type ScientificNotationNodeOptions = SelfOptions & StrictOmit<NodeOptions, 'children'>;
export type ToScientificNotationOptions = Pick<ScientificNotationNodeOptions, 'mantissaDecimalPlaces' | 'exponent'>;
export type ScientificNotation = {
    mantissa: string;
    exponent: string;
};
export default class ScientificNotationNode extends Node {
    readonly valueProperty: TReadOnlyProperty<number | null>;
    private readonly options;
    private readonly mantissaXSpacing;
    private readonly capHeight;
    private readonly mantissaNode;
    private readonly timesTenNode;
    private readonly exponentNode;
    private readonly disposeScientificNotationNode;
    constructor(valueProperty: TReadOnlyProperty<number | null>, providedOptions?: ScientificNotationNodeOptions);
    dispose(): void;
    private update;
    /**
     * Converts a number to scientific-notation format: M x 10^E, with mantissa M and exponent E.
     */
    static toScientificNotation(value: number, providedOptions?: ToScientificNotationOptions): ScientificNotation;
}
export {};
