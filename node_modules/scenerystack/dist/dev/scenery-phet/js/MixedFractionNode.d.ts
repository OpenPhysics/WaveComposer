export default MixedFractionNode;
declare class MixedFractionNode extends HBox {
    /**
     * @param {Object} [options]
     */
    constructor(options?: Object);
    wholeText: Text;
    numeratorText: Text;
    denominatorText: Text;
    wholeContainer: Text | AlignBox;
    numeratorContainer: Text | AlignBox;
    denominatorContainer: Text | AlignBox;
    vinculumNode: Line;
    vbox: VBox;
    _whole: any;
    _numerator: any;
    _denominator: any;
    _vinculumExtension: any;
    /**
     * Updates the view of the fraction when something changes.
     * @private
     */
    private update;
    /**
     * Sets the whole-number part of the mixed fraction.
     * @public
     *
     * @param {number|null} value
     */
    public set whole(value: number | null);
    /**
     * Returns the current whole-number part of the mixed fraction.
     * @public
     *
     * @returns {number|null}
     */
    public get whole(): number | null;
    /**
     * Sets the numerator part of the mixed fraction.
     * @public
     *
     * @param {number|null} value
     */
    public set numerator(value: number | null);
    /**
     * Returns the current numerator part of the mixed fraction.
     * @public
     *
     * @returns {number|null}
     */
    public get numerator(): number | null;
    /**
     * Sets the denominator part of the mixed fraction.
     * @public
     *
     * @param {number|null} value
     */
    public set denominator(value: number | null);
    /**
     * Returns the current denominator part of the mixed fraction.
     * @public
     *
     * @returns {number|null}
     */
    public get denominator(): number | null;
}
import HBox from '../../scenery/js/layout/nodes/HBox.js';
import Text from '../../scenery/js/nodes/Text.js';
import AlignBox from '../../scenery/js/layout/nodes/AlignBox.js';
import Line from '../../scenery/js/nodes/Line.js';
import VBox from '../../scenery/js/layout/nodes/VBox.js';
