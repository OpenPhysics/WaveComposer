/**
 * key object, intended for use in the PhET common-code keypad
 *
 * @author Aadish Gupta
 * @author John Blanco
 */
import '../../../globals.js';
import { OneKeyStroke } from '../../../scenery/js/input/KeyDescriptor.js';
import Node from '../../../scenery/js/nodes/Node.js';
import { KeyIDValue } from './KeyID.js';
type SelfOptions = {
    horizontalSpan?: number;
    verticalSpan?: number;
    keyboardIDs?: OneKeyStroke[];
};
export type KeyOptions = SelfOptions;
declare class Key {
    readonly label: Node | string;
    readonly keyID: KeyIDValue;
    readonly horizontalSpan: number;
    readonly verticalSpan: number;
    readonly buttonTandemName: string;
    readonly keyboardIDs: OneKeyStroke[];
    /**
     * @param label - node or string that will appear on the key
     * @param keyID - ID for this key, see KeyID.js
     * @param [providedOptions]
     */
    constructor(label: Node | string, keyID: KeyIDValue, providedOptions?: KeyOptions);
}
export default Key;
