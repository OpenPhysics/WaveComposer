// Copyright 2021-2025, University of Colorado Boulder
/**
 * Property support for rich enumeration types.
 *
 * @author Sam Reid (PhET Interactive Simulations)
 */
import '../../globals.js';
import optionize from '../../phet-core/js/optionize.js';
import EnumerationIO from '../../tandem/js/types/EnumerationIO.js';
import axon from './axon.js';
import Property from './Property.js';
export default class EnumerationProperty extends Property {
    constructor(value, providedOptions) {
        const firstOptions = optionize()({
            enumeration: value.enumeration
        }, providedOptions);
        const options = optionize()({
            validValues: firstOptions.enumeration.values,
            phetioValueType: EnumerationIO({
                enumeration: firstOptions.enumeration
            })
        }, firstOptions);
        super(value, options);
    }
}
axon.register('EnumerationProperty', EnumerationProperty);
