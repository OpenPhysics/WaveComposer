"use strict";
exports.__esModule = true;
var trie_data_1 = require("./trie-data");
var unicode_trie_1 = require("./unicode-trie");
var classes_1 = require("./classes");
var pairs_1 = require("./pairs");
var classTrie = new unicode_trie_1["default"](trie_data_1["default"]);
function mapClass(c) {
    switch (c) {
        case classes_1.AI:
            return classes_1.AL;
        case classes_1.SA:
        case classes_1.SG:
        case classes_1.XX:
            return classes_1.AL;
        case classes_1.CJ:
            return classes_1.NS;
        default:
            return c;
    }
}
function mapFirst(c) {
    switch (c) {
        case classes_1.LF:
        case classes_1.NL:
            return classes_1.BK;
        case classes_1.CB:
            return classes_1.BA;
        case classes_1.SP:
            return classes_1.WJ;
        default:
            return c;
    }
}
var Break = /** @class */ (function () {
    function Break(position, spacePos, required) {
        if (required === void 0) { required = false; }
        this.position = position;
        this.spacePos = spacePos;
        this.required = required;
    }
    return Break;
}());
exports.Break = Break;
var LineBreaker = /** @class */ (function () {
    function LineBreaker(node) {
        this.node = node;
        this.pos = 0;
        this.lastPos = 0;
        this.curClass = null;
        this.nextClass = null;
    }
    LineBreaker.prototype.nextCodePoint = function () {
        var code = this.node.charCodeAt(this.pos++);
        var next = this.node.charCodeAt(this.pos);
        // if a surrogate pair
        if (0xd800 <= code && code <= 0xdbff && (0xdc00 <= next && next <= 0xdfff)) {
            this.pos++;
            return (code - 0xd800) * 0x400 + (next - 0xdc00) + 0x10000;
        }
        return code;
    };
    LineBreaker.prototype.nextCharClass = function (first) {
        if (first === void 0) { first = null; }
        if (first == null) {
            first = false;
        }
        return mapClass(classTrie.get(this.nextCodePoint()));
    };
    LineBreaker.prototype.nextBreak = function () {
        var lastSpacePos = null;
        var lastClass = null;
        var shouldBreak = false;
        // get the first char if we're at the beginning of the string
        if (this.curClass === null) {
            this.curClass = mapFirst(this.nextCharClass());
        }
        while (this.pos < this.node.length) {
            this.lastPos = this.pos;
            lastClass = this.nextClass;
            this.nextClass = this.nextCharClass();
            // explicit newline
            if (this.curClass === classes_1.BK || (this.curClass === classes_1.CR && this.nextClass !== classes_1.LF)) {
                this.curClass = mapFirst(mapClass(this.nextClass));
                return new Break(this.lastPos, this.lastPos, true);
            }
            // handle classes not handled by the pair table/
            var cur = null;
            switch (this.nextClass) {
                case classes_1.SP:
                    cur = this.curClass;
                    break;
                case classes_1.BK:
                case classes_1.LF:
                case classes_1.NL:
                    cur = classes_1.BK;
                    break;
                case classes_1.CR:
                    cur = classes_1.CR;
                    break;
                case classes_1.CB:
                    cur = classes_1.BA;
                    break;
                default:
                    cur = null;
                    break;
            }
            // Trigger lastSpacePos when going from nonspace->space
            if (this.nextClass === classes_1.SP && lastClass !== classes_1.SP) {
                lastSpacePos = this.lastPos;
            }
            if (cur !== null) {
                this.curClass = cur;
                if (this.nextClass === classes_1.CB) {
                    return new Break(this.lastPos, lastSpacePos !== null ? lastSpacePos : this.lastPos);
                }
                continue;
            }
            // if not handled already, use the pair table
            switch (pairs_1.pairTable[this.curClass][this.nextClass]) {
                case pairs_1.DI_BRK: // direct break
                    shouldBreak = true;
                    break;
                case pairs_1.IN_BRK:
                    shouldBreak = lastClass === classes_1.SP;
                    break;
                case pairs_1.CI_BRK:
                    shouldBreak = lastClass === classes_1.SP;
                    if (!shouldBreak) {
                        continue;
                    }
                    break;
                case pairs_1.CP_BRK: // prohibited for combining marks
                    if (lastClass !== classes_1.SP) {
                        continue;
                    }
                    break;
                default:
                    break;
            }
            // Reset lastSpacePosition if a non-space character encountered
            // Prevent swallowing "]swallowed"
            if (!shouldBreak && this.nextClass !== classes_1.SP)
                lastSpacePos = null;
            this.curClass = this.nextClass;
            if (shouldBreak) {
                return new Break(this.lastPos, lastSpacePos !== null ? lastSpacePos : this.lastPos);
            }
        }
        if (this.pos >= this.node.length) {
            if (this.lastPos < this.node.length) {
                this.lastPos = this.node.length;
                return new Break(this.node.length, lastSpacePos !== null ? lastSpacePos : this.node.length);
            }
            else {
                return null;
            }
        }
    };
    return LineBreaker;
}());
exports.LineBreaker = LineBreaker;
